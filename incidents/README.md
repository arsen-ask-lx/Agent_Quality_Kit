# Журнал шишек

> **Что это.** Одно место на все проекты, куда записывается каждый обнаруженный недочёт **процесса
> или прибора** — и что из него сделано. Пополняется командой `aqk note "..."` из любого проекта.
>
> **Что сюда НЕ пишется:** продуктовые дефекты («отчёт считает неправильно»). Их место — в очереди
> работ конкретного проекта. Здесь только про то, **как мы работаем**, а не про то, **что построили**.
>
> **Петля.** Шишка, ставшая машинной проверкой, помечена ✅ и названа поимённо — так видно, что
> журнал не архив, а источник гейтов в `kit/gates/`.
>
> **Правило записи:** каждая запись несёт вывод — 🔧 гейт (машина) · 📜 правило (дисциплина) ·
> 👤 решение владельца. Урок без вывода — история, а не урок.
>
> **Повтор того же класса — не новая запись, а усиление старой:** правило → гейт.
>
> **Запись пишется в тот же день.** Если вывода ещё нет — так и пометить `⬜ вывода нет`: это
> значит «недочёт не разобран», а не «недочёта не было.»

---

## Перенесённое из audit_project — стартовый набор

Записи ниже перенесены 2026-09-02 из проекта `audit_project` (Django + React, ~год работы с
агентами). Это **не примеры формата** — это настоящие шишки, каждая стоила времени. Они здесь,
чтобы следующий проект не набивал их заново.

---

## 2026-08-24 — гейт краснел на том написании, которое сам предписывал

> ✅ **Стало гейтом:** `kit/gates/gate-has-samples` — запись каталога не принимается без
> красного и зелёного образца, и оба прогоняются при каждой проверке.

**Проект:** audit_project
**Класс:** гейт соврал

**Что случилось.** Правило статического анализа запрещало создавать десятичное число из числа с
плавающей точкой и предписывало писать строкой. При этом оно ругалось на правильное написание
строкой — причём не всегда, а в зависимости от соседнего кода в файле: инструмент местами отдавал
содержимое строки без кавычек, и проверка не узнавала своё же предписание.

**Чем это стоило.** Красный конвейер, полчаса на локализацию. Круг конвейера — 15–18 минут.

**Вывод.** 🔧 У каждого гейта обязаны быть **два образца**: красный (код, где гейт обязан
краснеть) и **зелёный** (правильный код, где обязан молчать), оба в репозитории, оба прогоняются
вместе с гейтом. Зелёный важнее: он поймал бы эту ошибку в день установки правила. Гейт без обоих
образцов не считается работающим.

---

## 2026-08-24 — правка конфига гейта не создавала конвейер вообще

> ✅ **Стало гейтом:** `kit/gates/gates-run-in-ci` — объявленный гейт, которого нет в конвейере,
> красит проверку. Отдельно: «конвейера нет» и «конвейер зелёный» теперь выглядят по-разному.

**Проект:** audit_project
**Класс:** гейт молчал

**Что случилось.** Пути к конфигам проверок не были в белом списке конвейера. Правка конфига не
роняла сборку — сборки **не было вовсе**. Вместе с ней не появлялось и кнопки выката, то есть
работа стояла молча.

**Чем это стоило.** Тишина читалась как «всё в порядке». Класс худший из возможных: отсутствие
сигнала неотличимо от успеха.

**Вывод.** 🔧 Конфиг гейта обязан быть в списке путей, запускающих конвейер. Отдельно: «конвейера
нет» и «конвейер зелёный» обязаны выглядеть по-разному.

---

## 2026-08-27 — прибор завышал память в 11 раз, замеры пошли бы в решения

> 👤 **Гейтом не станет:** правило про порядок замера, а не про содержимое репозитория. Машине
> не видно, поверяли прибор до замера или нет. Живёт правилом в `kit/docs/`.

**Проект:** audit_project
**Класс:** прибор соврал

**Что случилось.** Пик памяти процесса снимали счётчиком, который копит исторический максимум
вместе с кешем файлов. На пустом контейнере он показал 438 МиБ против 38 МиБ реально занятых —
**завышение в 11 раз**. Предыдущая оценка нагрузки была завышена втрое и на её основе чуть не
развели очереди.

**Чем это стоило.** Один час замеров в мусор; вторая, независимая ошибка того же дня — счётчик
очереди опрашивали не у того сервера (их два, и неиспользуемый бодро отвечал «0», неотличимо от
правды), из-за чего родилась ложная тревога «счётчик слеп».

**Вывод.** 📜 Прибор проверяется на заведомо известном состоянии **до** замера. Ошибка прибора не
даёт честно неверного ответа — она даёт **правдоподобный**, по которому потом принимают решения.
🔧 Протокол замера живёт файлом в репозитории, а не в переписке: в версии, жившей в переписке,
нашлось четыре ошибки.

---

## 2026-08-24 — документ четыре недели утверждал защиту, которой не было

> ✅ **Стало гейтом:** `kit/gates/entry-links-exist` — ссылка из свода правил на несуществующий
> файл красит проверку.

**Проект:** audit_project
**Класс:** документ соврал

**Что случилось.** Чек-лист гейтов числил работающими изоляцию исполнителя и генерацию контрактов.
Обе отключены решением владельца за три недели до этого, потому что инструмент ломал файлы.

**Чем это стоило.** Планирование опиралось на защиту, которой нет.

**Вывод.** 📜 Статус берётся из кода и замеров, а не из заголовка документа. Расхождение даты в
шапке файла с последним коммитом больше недели — документ читать нельзя. 🔧 Отдельный гейт:
файлы, на которые ссылается корневая инструкция, обязаны существовать.

---

## 2026-08-26 — красный конвейер после пуша ждал владельца

> 👤 **Гейтом не станет:** правило поведения агента, а не свойство кода. Гейт в репозитории не
> может знать, смотрел ли кто-то прогон. Живёт в `kit/rules/general.md`.

**Проект:** audit_project
**Класс:** правило обошли

**Что случилось.** Пятнадцать коммитов, красные тесты в конвейере. Агент ждал, что о поломке
сообщит человек со скриншотом.

**Чем это стоило.** Кнопки выката не появляется при красном вовсе — работа стояла молча.
Слова владельца: «я не должен говорить тебе о нём — ты должен сразу это делать после моего пуша».

**Вывод.** 📜 После пуша конвейер до зелёного доводит агент: смотрит прогон сам, разбирает лог,
чинит, просит перепушить. Не ждать сообщения о поломке.

---

## 2026-08-25 — четыре красных круга конвейера за день, три ловились локально

> 🔧 **Стало правкой оснастки:** `doctor --run` гоняет локально ровно то, что объявлено в
> манифесте и потому едет в конвейер; `smoke.sh` последней строкой печатает, чего не проверил.

**Проект:** audit_project
**Класс:** гейт молчал (не там, где нужен)

**Что случилось.** Часть проверок жила **только** в конвейере: в локальных хуках их не было, а
«прогнать всё как CI» означало десяток команд, которые надо помнить.

**Чем это стоило.** Четыре круга по 15–18 минут за один день; три из четырёх падений ловились
локально за секунды.

**Вывод.** 🔧 Одна команда прогоняет локально то же, что конвейер, — и **честно печатает, чего не
проверила**. Инструмент, молчащий о своих пробелах, хуже отсутствующего: на него понадеются.
Тяжёлые шаги — перед пушем, а не на каждый коммит, иначе их начнут обходить.

---

## 2026-08-27 — инструмент забраковали по прогону без его настройки

> 🔧 **Стало правкой оснастки:** записи без переносимого рецепта едут с настройкой и полем
> `samples_for` — какой инструмент проверяет образцы. Мёртвый код у нас поставлен с `knip.json`,
> а не «из коробки»: без настройки он объявлял мёртвыми сами образцы.

**Проект:** audit_project
**Класс:** прибор соврал

**Что случилось.** Детектор мёртвого кода прогнали «из коробки», получили лавину ложных
срабатываний и удалили пакет как негодный. После настройки под фреймворк он оказался пригоден и
нашёл настоящую находку.

**Чем это стоило.** Инструмент удалён зря, работа сделана дважды. Рядом — вторая ошибка того же
дня: попытка «убрать шум», исключив из проверки файлы обработчиков, убрала бы не шум, а
**вызывающих**: 91 живая функция была бы объявлена мёртвой.

**Вывод.** 📜 Инструмент судят по прогону **с его штатной настройкой**, а не «из коробки».
📜 «Не используется» и «не применим» — разные утверждения; списывать инструменты скопом нельзя.

---

## 2026-08-05 — реализованный план переписали задним числом

> 👤 **Гейтом не станет:** решение владельца — правило дисциплины, а не хук. Записано, чтобы
> к этому спору не возвращаться.

**Проект:** audit_project
**Класс:** правило обошли

**Что случилось.** План со статусом «реализовано» отредактировали: вычеркнули один из критериев
приёмки. Отдельно: другой план приехал планом, кодом и отметкой «реализовано» одним коммитом —
одобрять было нечего.

**Чем это стоило.** Запись о случившемся перестала быть записью о случившемся; проверить, что
именно приняли, стало нечем.

**Вывод.** 📜 Реализованный план неизменяем: дописывать можно только журнал выполнения, новая
находка — новый план. План рождается **отдельным коммитом** и получает одобрение до кода.
👤 Механический гейт на это владелец делать отказался: правило дисциплины, а не хук.

---

## 2026-08-06 — отступление от принятого решения записали комментарием в коде

> 👤 **Гейтом не станет:** машине не отличить обоснованное отступление от самовольного. Живёт
> железным правилом «развилка — вопрос человеку, а не комментарий в коде» в `AGENTS.md`.

**Проект:** audit_project
**Класс:** правило обошли

**Что случилось.** Принятое архитектурное решение требовало собирать документ потоком. Реализация
поехала другим путём, потому что так вид совпадал точнее. Отступление автор знал, нашёл и записал
комментарием в коде — владелец обнаружил его через день, читая документацию.

**Чем это стоило.** Отложенная: выбранный путь держит файл в памяти целиком — плюс 435 МБ на
100 тысяч строк против 128 КБ. Разница проявится ровно тогда, когда данных станет много, то есть
когда переделывать дороже всего.

**Вывод.** 📜 Отступление от принятого решения — **вопрос владельцу, а не комментарий в коде**,
даже когда причина выглядит убедительной и результат нравится.

---

## 2026-07-29 — скилл не загрузился, согласованное решение переоткрыли заново

> 👤 **Гейтом не станет:** про оснастку агента, а не про репозиторий получателя. Живёт правилом
> «критичная процедура подключается явным указателем» в `kit/docs/`.

**Проект:** audit_project
**Класс:** правило обошли

**Что случилось.** По теме, у которой был скилл со всеми принятыми решениями, скилл не
загрузился. Вопрос, согласованный неделей раньше и лежавший в том же скилле, задали владельцу
повторно.

**Чем это стоило.** Время владельца на уже принятое решение; риск принять его иначе.

**Вывод.** 📜 Автоподхвата по ключевому слову **не существует** — скилл грузится решением агента.
Критичные процедуры подключаются явным указателем («перед задачей Y читай X»), а не надеждой на
описание. 📏 Замер индустрии: скиллы не срабатывают сами примерно в половине случаев, явный
указатель — почти всегда.

---

## 2026-08-27 — ревизия гейтов: замер каждого провокацией

> ✅ **Стало гейтом:** `kit/gates/gates-are-runnable` — объявленный арбитр обязан запускаться.
> 🔧 И правкой оснастки: `tool/selfcheck/gates.sh` принимает запись только по четырём фактам —
> запускается · краснеет на красном · говорит, что нашёл · молчит на зелёном (пункты 10 и 11).

**Проект:** audit_project
**Класс:** сводная ревизия

Однодневный заход «пройтись по каждому гейту и проверить, что он ловит». Метод: прочитать
правило → подсунуть нарушение → убедиться, что покраснело → положить образец в репозиторий →
**сломать гейт нарочно** и убедиться, что образец покраснел снова.

### Найденные недочёты приборов

| # | Недочёт | Вывод |
|---|---|---|
| 1 | **Правило неразрывного пробела не ловило ничего.** `grep -P '\xc2\xa0'` сопоставляет байты только под `LC_ALL=C`; в UTF-8-локали это два ДРУГИХ символа. Правило числилось в списке защит годами. | 🔧 разбор перенесён в python, образцы на трёх локалях  |
| 2 | **Гейт сырого `request.*` не видел пакеты.** Проверялись только файлы `views.py`/`viewsets.py`; каталог `views/` с модулями внутри проходил мимо. | 🔧 область расширена на пакеты + реестр долга на 17 находок  |
| 3 | **Сверка свежести схемы API не блокировала ничего.** Стояла внутри advisory-обёртки фаззера, сцепленная `&&`: расхождение роняло цепочку, конвейер печатал «ADVISORY: API fuzz baseline remains» и ехал зелёным — то есть ещё и называл чужую причину. | 🔧 вынесена в отдельный блокирующий шаг + регресс-тест  · 📜 **блокирующую проверку нельзя сцеплять `&&` с необязательной: обёртка сильнее содержимого** |
| 4 | **Необязательные проверки бессрочны.** Шесть штук печатали в пустоту; «потом решим» не наступало. | 🔧 реестр, где у каждой записано УСЛОВИЕ конца, а не оправдание  |
| 5 | **Мёртвые функции бэкенда не искал никто.** Импорты закрыты `ruff`, фронт закрыт `knip`, бэкенд пусто. | 🔧 `vulture` + настройка Django/DRF + реестр 148 записей  |
| 6 | **Сложность функций не мерил никто.** 75 функций сложнее нормы, худшая 29 ветвлений в контуре целостности файлов. | 🔧 храповик над `ruff C901`  |
| 7 | **Пакеты без потребителя числились защитой.** Девять dev-зависимостей не звал никто; `freezegun` числился заморозкой времени там, где её не делают. | 🔧 гейт «у dev-зависимости есть потребитель», бэк + фронт  |

### Найденные недочёты собственного метода — они дороже

| # | Недочёт | Вывод |
|---|---|---|
| 10 | **Провокация, которая не краснеет, проверяется САМА.** Трижды подряд мои образцы были неверны: линтер миграций запускался без `DJANGO_ENV=test`; модель без такой таблицы; модель `AppCapability`, у которой `sqlmigrate` даёт `(no-op)` — линтеру нечего было смотреть, и он честно сказал «валидно». Я чуть не записал это в дефект гейта. | 📜 не краснеет — сперва доказать, что образец вообще нарушает правило |
| 11 | **RED проверяется по СОДЕРЖАНИЮ вывода, не по коду возврата.** Отсутствующий скрипт тоже даёт ненулевой код: тест «упал = хорошо» зелёный и при пустом гейте. | 📜 в каждой провокации — проверка текста отказа. ✅ **Закрыто в AQK:** `tool/selfcheck/gates.sh` различает «арбитр не запускается», «промолчал на красном» и «покраснел молча» |
| 12 | **Мутации гоняются по одной.** Пачка из четырёх в одной команде упёрлась в лимит времени и оставила код сломанным. | 📜 одна мутация — одна команда — восстановление |
| 13 | **«У пакета появился потребитель» ≠ «пакет приносит пользу».** Чтобы удовлетворить собственный новый гейт, я завёл `vulture` строкой в конвейер, не проверив, исполним ли его совет. Гейт проверяет первое; второе проверяю я. | 📜 записано в план отдельным пунктом |
| 14 | **Область гейта решается замером, а не планом.** План исключал `scripts/`; замер показал, что там лежит сложная функция — гейт, написанный в тот же день. Харнес живёт по правилу, которое сторожит. | 📜 |

---

## Открытые работы audit_project на 2026-08-27

Это **не уроки** — у них нет вывода, и по правилу этого журнала их бы сюда не пустили. Оставлены
как контекст: видно, какие дыры остаются в проекте, который прошёл ревизию гейтов.

| # | Дыра | Состояние |
|---|---|---|
| 15 | **Агенты ссылаются на файлы, которых нет:** `specs/memory/learnings/`, `specs/memory/metrics.json`, `specs/memory/evolution-archive.jsonl`. Упоминаются минимум в шести определениях агентов (`archivist`, `builder-frontend`, `jittest`, `planner`, `sentinel`). То есть петля «Analyzer собирает уроки → Evolver улучшает промпты» описана в правилах и **не существует**. | ⬜ решение владельца: либо поднять сбор, либо вычеркнуть петлю из правил. правила харнеса правятся отдельной задачей |
| 16 | **Наблюдаемости самого харнеса нет.** Ни трейсов шагов агента, ни учёта стоимости прогона, ни прогонщика эвалов скиллов (17 датасетов лежат с 2026-06-16 без прогонщика). Langfuse НЕ «работает и не используется» — он удалён вместе с headless-пайплайном, в `docker-compose` его нет ни в одном файле. | ⬜ отдельная задача; предпосылка — решение по п.15 |
| 17 | **181 уязвимость `pip-audit` на бэкенде и 28 на фронте** (4 критические, 18 высоких) печатались в конвейере всё это время. | ⬜ занесены в реестр необязательных проверок с условием разбора |
| 18 | **Размер бандла уже мог бы блокировать** (JS 1.06 из 1.1 МБ, CSS 67.55 из 80 КБ) и не блокирует. | ⬜ единственная из шести advisory, чей перевод в блок не требует подготовки |

---

## 2026-09-03 — зелёный конвейер на исправном коде ничего не доказывал

> 🔧 **Стало правкой оснастки:** у конвейера появился красный образец — нарочно сломанная
> программа в отдельной ветке. Прогон упал на первом шаге за 13 секунд, ветка удалена.

**Проект:** aqk
**Класс:** проверка не проверена

**Что случилось.** Конвейер собран, прогон зелёный за 17 секунд. Этого достаточно, чтобы
поверить, что он работает, — и недостаточно, чтобы это знать. Зелёный на исправном коде
одинаково выглядит и у работающего конвейера, и у конвейера, который не запускает ничего.

**Чем это стоило.** Ничем — поймано в тот же час. Но именно так в `audit_project` четыре недели
жила проверка, которая не создавала сборку вовсе.

**Вывод.** 🔧 У конвейера, как у любого гейта, обязан быть красный образец: нарочно сломанная
программа в отдельной ветке, предложение изменений, прогон обязан упасть. Проверено — падает на
первом шаге за 13 секунд. 📜 «Прогон зелёный» — не доказательство работоспособности проверки;
доказательство — прогон, который покраснел там, где должен.

---

## 2026-08-25 — разбор 1069 коммитов: чем кончается работа без упоров

> ✅ **Стало гейтами:** `file-size-limit` · `swallowed-error` · `todo-without-task` — те самые
> три упора, которых там не было. Все три переносимы между языками.

**Проект:** чужой, разбор опубликован практиком; перенесено сюда как задокументированный отказ
**Класс:** упора не было

**Что случилось.** Автор проанализировал git-историю своего проекта за 90 дней работы с агентами:

- из **1069 коммитов 416 — это `fix`**: не фича, не переработка, а стабилизация уже выкаченного;
- **1436 блоков «поймать любую ошибку»**, из которых лишь около 10% пробрасывают её дальше;
  остальные глушат в лог или возвращают пустоту;
- **один файл маршрутов на 6305 строк** — «не потому что так задумано, а потому что каждую новую
  фичу дописывали туда же: агенту так ближе по контексту»;
- число моделей захардкожено в четырёх местах четырьмя разными значениями;
- пиковая скорость 44 коммита в день — «это не человеческий темп ревью».

**Чем это стоило.** Три недели поиска источника отказов, которые глушились на месте. Диагноз
автора: «долг копится строго там, где я убрал себя из центра».

**Вывод.** 🔧 Три упора, которых там не было и которых у нас тоже не было: предел размера файла ·
запрет тихо проглоченной ошибки · маркеры «доделать потом» без заведённой задачи. Все три
проверяются машиной и переносятся между языками. 📜 Агент множит и продуктивность, и долг с
одинаковым КПД: без упоров множится второе.

---

## 2026-08-27 — сборка невоспроизводима: версии не закреплены

> ✅ **Стало гейтом:** `kit/gates/deps-are-pinned` — рядом с объявлением зависимостей обязан
> лежать файл с закреплёнными версиями.

**Проект:** audit_project
**Класс:** упора не было

**Что случилось.** Обязательный минимум требует точных версий всего, что участвует в сборке.
Проверки на это не стояло нигде: наличие файла с закреплёнными версиями рядом с объявлением
зависимостей никто не сверял.

**Чем это стоило.** Прямой цены не заплачено — поймано осмотром. Класс дефекта известен: среда
разъезжается между машиной разработчика, конвейером и сервером, и «у меня работает» становится
неопровержимым.

**Вывод.** 🔧 Гейт: рядом с объявлением зависимостей обязан лежать файл с закреплёнными версиями.

---

## 2026-09-03 — сторожа не пережили встречу с настоящим проектом

> 🔧 **Стало правкой оснастки:** общий список исключений `kit/gates/_skip.sh`, единый для всех
> проверок и едущий в проект вместе с гейтом; у каждой проверки — свой список мест, где её
> конструкция законна. Размер файла: 120 секунд → 0.13.

**Проект:** aqk, прогон по audit_project (36 664 файла)
**Класс:** гейт врал объёмом

**Что случилось.** Шесть проверок, зелёных на своих образцах, впервые запустили по живому
проекту. Результат:

- проверка размера файла **не уложилась в две минуты** — цикл с подсчётом строк на каждый файл;
- из 1831 её находки **1722 пришли из чужого кода в окружении** (`.venv/site-packages`);
- проверка отладочной печати дала 330 находок, из которых **285 — из `scripts/` и оснастки**,
  где печать это способ говорить с человеком, а не забытая отладка.

**Чем это стоило.** Ничем — поймано до того, как этим кто-то воспользовался. Но человек,
получивший такой отчёт, выключил бы проверки целиком и был бы прав: 94% и 86% шума соответственно.

**Вывод.** 🔧 Один обход вместо цикла: 120 секунд → 0.13 секунды. 🔧 Общий список исключений
`_skip.sh` — один на все проверки, едет в проект вместе с гейтом; разъехавшись, он окажется
полным в одной проверке и дырявым в другой. 🔧 Отдельно от него — свой список у каждой проверки:
места, где именно эта конструкция законна. Секрет в `scripts/` — такой же секрет, а печать —
нет. 📜 **Гейт проверяется не на образцах, а на настоящем репозитории.** Образцы доказывают, что
он ловит и молчит; только живой прогон показывает, сколько в его отчёте шума.

---

## 2026-09-03 — час работы на то, что делает одна строка конфига

> 🔧 **Стало правкой оснастки:** карта готовых правил `kit/docs/ready-made-rules.md` (964 правила
> `ruff`, инструменты по языкам) и обязательный раздел «готовый аналог» в README каждой записи:
> есть или нет и что именно проверено.

**Проект:** audit_project
**Класс:** свой гейт вместо готового

**Что случилось.** Разбор синтаксического дерева по всему проекту искал места, где ошибку
записывают в лог без трейса. Час работы, 80 находок, отдельная задача в очереди. Существует
готовое правило `TRY400`, которое находит их одной строкой в конфиге и точнее самописного
разбора: оно нашло 47 мест и не дало ложных.

Замер по проекту показал, что это не единичный случай. Самописный запрет отладочной печати
дублирует `T20` (107 находок), самописный поиск маркеров долга — `FIX` и `TD`, отдельный аудитор
безопасности целиком перекрыт группой `S`. Из 964 правил `ruff` в проекте включено около
семнадцати групп — примерно два процента.

**Чем это стоило.** Час прямой работы плюс годы поддержки трёх самописных проверок, у которых
всё это время был готовый и более точный аналог.

**Вывод.** 📜 Прежде чем писать свой гейт — искать готовое правило: `ruff`, `eslint`, `semgrep`.
Готовое лучше не потому, что чужое, а потому что его поддерживают без нас, оно подробнее и его
знают в лицо в чужих проектах. 📜 Обратная половина обязательна, иначе правило превращается в
«всё делают библиотеки»: **свой гейт законен, когда он про решение конкретного проекта** —
паритет очередей, свежесть карты, неизменяемость миграции. 🔧 В README каждой записи каталога —
раздел «готовый аналог»: есть или нет, и что именно проверено. Проверено на неразрывном пробеле:
его не ловит **ни одно** из 964 правил, там свой гейт единственный способ. «Не искал» и «нет» —
разные утверждения.

---

## 2026-09-03 — второй настоящий проект: шума было втрое больше, чем находок

> 🔧 **Стало правкой оснастки:** опознание сгенерированного файла по шапке, поиск только в файлах
> кода, мягче предел вложенности для разметки, дубли не ищутся в тестах, повтор печатается один
> раз вместо раза на каждое окно сдвига.

**Проект:** aqk, прогон по Newsletter (414 файлов, Python + TypeScript)
**Класс:** гейт врал материалом

**Что случилось.** Восемь проверок, уже переживших прогон по проекту в 36 тысяч файлов,
запустили по второму — меньшему и на другом стеке. Из 55 находок настоящими оказались 16.

Четыре источника шума, и все четыре — про **природу материала**, а не про объём:

| Что | Пример |
|---|---|
| **сгенерированные файлы** | `schema.d.ts` на 2260 строк и 438 самоповторов — его никто не пишет руками |
| **не код** | пример в `.env.example`, отчёт тестов в HTML, скрипт сборки документации |
| **файлы самих гейтов** | копия проверок в `gates-reference/` содержит искомые слова в своих шаблонах |
| **разметка вместо логики** | шесть уровней вложенности в компоненте — это вёрстка, а не сложность |
| **тесты** | все двадцать находок дублей были в тестах, где повтор часто осознанный |

**Чем это стоило.** Ничем — поймано до того, как кто-то этим воспользовался. Но человек,
которому показали бы 55 находок с 16 настоящими, выключил бы проверки.

**Вывод.** 🔧 Опознание сгенерированного по шапке файла — общее для всех проверок. 🔧 Печать и
маркеры долга ищутся **только в файлах кода**, а не во всём подряд: те же слова в настройках и
разметке означают другое. 🔧 Разметке предел вложенности мягче на три уровня. 🔧 Дубли не
проверяются в тестах — этот случай решают правила тестирования, а не гейт. 🔧 Повторённый кусок
печатается один раз, а не по разу на каждое окно сдвига.
📜 **Двух проектов мало.** Первый показал, что мешает объём; второй — что мешает материал. Третий
покажет третье. Гейт считается зрелым не после образцов и не после одного прогона.

---

## 2026-09-03 — проверка зависала навсегда на проекте не на своём языке

> 🔧 **Стало правкой оснастки:** список файлов собирается до разбора, пустой — выход сразу;
> проверка 19 в `smoke.sh` гоняет все арбитры с открытым потоком ввода.

**Проект:** aqk, прогон по искусственным проектам на Go и Rust
**Класс:** гейт не отпускал

**Что случилось.** Проверка на проглоченную ошибку разбирала файлы через `awk`, получая список
файлов подстановкой. На проекте, где нет файлов её языков, список оказывался пустым — и `awk`
**без файловых аргументов начинал читать поток ввода** и ждал его вечно.

В терминале это незаметно: поток закрыт, программа сразу заканчивает. Заметно там, где поток
открыт — **в конвейере и в хуке коммита**. Прогон встал на две минуты и был снят по времени.

**Чем это стоило.** Ничем — поймано на искусственном проекте. Но человек с проектом на Go получил
бы намертво зависший хук коммита и не понял бы, почему.

**Вывод.** 🔧 Список файлов собирается заранее, пустой — выход сразу; разбор идёт через `xargs`,
который на пустом входе не запускается вовсе. 📜 **Проверку надо гонять на проекте, где её
языков нет.** Тестирование «на своём материале» показывает только то, что она находит; ничего не
находить она тоже обязана уметь — и делать это быстро.

Заодно выяснилось, что проверки не знали конструкций Go и Rust: печать (`fmt.Print`, `println!`),
проверенная и выброшенная ошибка, отброшенный результат. Добавлено вместе с образцами на обоих
языках — поддержка языка без образца ничем не доказана.

---

## 2026-09-03 — печать внутри комментария считалась печатью

> 🔧 **Стало правкой оснастки:** общий фильтр `drop_comments` в `_skip.sh` и `staticfiles`
> в общем списке исключений.

**Проект:** aqk, повторный прогон по audit_project (36 664 файла)
**Класс:** гейт врал материалом

**Что случилось.** Проверку отладочной печати, уже пережившую два настоящих проекта, прогнали
по первому из них ещё раз. Осталось 40 находок вместо прежних 330. Разбор каждой:

| Источник | Сколько | Настоящее? |
|---|---|---|
| корневой скрипт-утилита `snapshot.py` | 31 | нет — печать там интерфейс программы |
| пример в JSDoc и закомментированная строка | 3 | нет — комментарий не выполняется |
| собранная статика `staticfiles/` | 2 | нет — сгенерировано `collectstatic` |
| отладочный хук `useScrollDebugger.ts` во фронтенде | **5** | **да** |

**Чем это стоило.** Ничем: пять настоящих находок — забытый отладочный хук, печатающий размеры
окна в консоль пользователя. Но соотношение 5 к 40 снова подводило к тому, чтобы проверку выключить.

**Вывод.** 🔧 Общий фильтр `drop_comments` в `_skip.sh`: находка в строке, начинающейся с `//`,
`*`, `#` или `--`, отбрасывается. Проверкам, которые ищут именно в комментариях — маркеры долга —
он не нужен и не применяется. 🔧 `staticfiles` в общий список исключений. 📜 **Проверка,
переставшая шуметь на одном проекте, не перестала шуметь вообще.** Прогон по тому же проекту
после правок дешевле нового проекта и находит следующий слой шума: сначала мешал объём, потом
материал, теперь — форма записи. 📜 Печать в корневом скрипте-утилите закрывается храповиком,
а не исключением: список имён файлов разъедется с проектом, реестр — нет.

---

## 2026-09-03 — долг, который нельзя было погасить работой

> 🔧 **Стало правкой оснастки:** `gate-has-samples` требует образцы только от записей-сканеров;
> реестр долга на восемь строк вычеркнут целиком — он был дефектом меры, а не долгом.

**Проект:** aqk
**Класс:** гейт мерил не то, что называл

**Что случилось.** Запись «у каждого гейта есть красный и зелёный образец» требовала образцы от
**каждой** записи манифеста. Четыре собственных прогона комплекта — разбор синтаксиса, проверка
на чистой папке, проверка каталога, проверка доказательств — образцов иметь не могут: у них
арбитр внутри них самих. Восемь строк легли в реестр долга и лежали там как работа, которой
никогда не будет.

Заметили, когда добавили пятый такой прогон — модульные проверки функций программы. Храповик
исправно показал два новых нарушения, и стало видно: каждая новая честная проверка **увеличивает
долг**. Мера, при которой правильное действие ухудшает показатель, измеряет не то.

**Чем это стоило.** Ничем в деньгах. Но реестр долга успел приучить: «эти восемь строк трогать
не надо, они такие». Реестр, в котором лежит невыполнимое, перестают читать целиком.

**Вывод.** 🔧 Образцы требуются только от записей-сканеров — тех, чья команда запускает проверку
из каталога образцов. Прогон с собственным арбитром (`pytest`, `node --test`, `go test`) из-под
правила выведен явно, и в README записи сказано, чего оно теперь не ловит. 📜 **Долг, который
нельзя погасить работой, — это не долг, а неверная мера.** Прежде чем записывать нарушение в
реестр, спроси: какой работой эта строка вычёркивается? Нет ответа — чинить надо проверку.
📜 Признак верной меры: правильное действие улучшает показатель. Если добавление честной
проверки увеличивает долг — мера сломана.

---

## 2026-09-03 — журнал рассказывал, но не отчитывался

> ✅ **Стало гейтом:** `kit/gates/lesson-has-outcome` — шишка без отметки о решении красит
> проверку. Отметок три, и отказ «гейтом не станет» — тоже решение.

**Проект:** aqk
**Класс:** документ соврал

**Что случилось.** В журнале лежало двадцать шишек. Отметку о том, во что каждая вылилась,
носили **две**. Из-за этого во всех отчётах о состоянии повторялась строка «шишек 18, гейтов из
них 2» — и она читалась как «петля не работает».

Разбор каждой записи показал другое:

| Чем кончилась | Сколько |
|---|---|
| ✅ стала записью каталога | 6 |
| 🔧 стала правкой самой оснастки | 9 |
| 👤 гейтом не станет — правило поведения или дисциплины | 5 |

Шесть последних — про то, смотрел ли кто-то прогон, поверяли ли прибор, спросили ли владельца.
Гейта из них не сделать в принципе, и это надо было сказать вслух, а не оставлять пустым местом.

**Чем это стоило.** Ничем в деньгах. Но полтора месяца собственный отчёт занижал результат втрое,
и решения о том, чем заняться дальше, принимались по этой цифре.

**Вывод.** 🔧 Отметка о решении обязательна и проверяется машиной. 📜 **Отказ — тоже решение, и
он обязан быть записан.** Пустое место на месте отказа неотличимо от забывчивости, и через
полгода спор открывают заново. 📜 Цифра в отчёте о собственном проекте — такой же прибор, как
любой другой: её поверяют до того, как по ней принимают решения. Эта не была поверена ни разу.

---

## 2026-09-03 — один список, переписанный четыре раза, ослеп на собственной программе

> 🔧 **Стало правкой оснастки:** общий отбор файлов кода `only_code` в `_skip.sh`; проверка
> обязательных разделов README в `tool/selfcheck/gates.sh`.

**Проект:** aqk
**Класс:** гейт молчал

**Что случилось.** Обход репозитория файл за файлом. Список расширений кода был переписан внутри
каждой сканирующей проверки заново — и разошёлся:

| Проверка | Что смотрела |
|---|---|
| размер файла | py js jsx ts tsx go rb java cs php **rs vue** |
| сложность | py js jsx ts tsx go rb java cs php |
| дубли | py js jsx ts tsx go rb java cs php |
| проглоченная ошибка | py js jsx ts tsx java cs rb php go rs |
| общий список `CODE_EXT` | те же **плюс mjs cjs mts cts kt swift scala** |

`.mjs` не было ни в одном. Программа самого комплекта целиком в этом расширении: предел размера
файла показывал зелёный на файле в **1366 строк при пределе 500**, и показывал бы дальше.

Как только проверки прозрели, наш собственный гейт дублей нашёл в программе настоящий дубль:
выбор рецепта был скопирован в `doctor` и в `add`. Правка доехала бы до одной копии.

Там же: четыре записи каталога из четырнадцати жили без раздела «готовый аналог», четыре — без
«чего НЕ ловит». Оба раздела обязательны нормой каталога полтора месяца.

**Чем это стоило.** Ничем — поймано на своём же репозитории. Но проверка размера файла числилась
работающей с первого дня и не проверяла ничего на главном файле проекта.

**Вывод.** 📜 **Список, переписанный в двух местах, — уже разошедшийся список.** Вопрос только
когда. 📜 Правило нормы, за которым не следит машина, — это пожелание: четыре записи нарушали
письменное требование полтора месяца, и никто не заметил. 🔧 Единый отбор `only_code` и проверка
обязательных разделов README в фильтре каталога — доказана провокацией: раздел убран, запись
отклонена.

---

## 2026-09-03 — храповик вписывал новые нарушения в долг заодно с исправленными

> 🔧 **Стало правкой оснастки:** в реестр пишется пересечение старого списка с текущим, а не
> текущий список целиком; проверка 26 в `smoke.sh`.

**Проект:** aqk
**Класс:** гейт врал устройством

**Что случилось.** Программу разделили на восемь файлов — она переросла собственный предел в 500
строк. Печать в ней записана долгом, и ключи реестра содержат имена файлов: после разделения 64
записи исчезли, 66 появились на новых путях.

Храповик отработал правильно — покраснел. Но при этом **переписал реестр текущим списком**, то
есть внёс в долг и все 66 новых записей. Второй прогон стал зелёным.

Правило было такое: «если что-то исправлено — перепиши реестр». Значит ответ на главный вопрос
«может ли новый код добавить нарушение и пройти?» был **«да, если заодно починить одно старое»**.
Один красный прогон — и дальше зелено навсегда.

**Чем это стоило.** Ничем: поймано в тот же час на своём же репозитории. Но дыра жила с первого
дня храповика и была невидима — все проверки гоняли по одному разу.

**Вывод.** 🔧 В реестр идёт пересечение `было ∩ сейчас`: исправленное вычёркивается, новое не
входит **никогда**. 📜 **Проверку механизма прогоняют дважды.** Первый прогон показывает, что
он краснеет; второй — что он краснеет **снова**. Между ними механизм успевает переписать своё
состояние, и именно там прячется разрешение, которое никто не выдавал. 📜 Разделение файла —
хорошая провокация для всей оснастки: пути меняются разом, и всё, что молча привязано к путям,
показывает себя.

---

## 2026-09-03 — ту же ошибку повторили в тот же день

> 🔧 **Стало правкой оснастки:** каталоги, где печать — интерфейс, объявляет проект в манифесте
> (`AQK_PRINT_OK_DIRS`), а не реестр долга.

**Проект:** aqk
**Класс:** гейт мерил не то, что называл

**Что случилось.** Утром вычеркнули восемь строк реестра с выводом «долг, который нельзя погасить
работой, — это не долг, а неверная мера». Вечером того же дня в реестре лежали 67 строк печати из
программы командной строки — печати, которую никто никогда не уберёт, потому что это её способ
выдавать результат.

Заметили не по выводу, а по трению: добавили команду `why`, и храповик покраснел на 34 новых
строках вывода. Каждая правка интерфейса требовала переснять реестр.

**Чем это стоило.** Ничем. Но вывод, записанный утром, не сработал вечером на том же материале:
правило было понято как «бывает такой случай», а не как **вопрос, который задают каждому реестру**.

**Вывод.** 🔧 Каталоги, где печать законна, называет сам проект строкой в манифесте — рядом с
командой, на виду. 📜 **Вывод из шишки становится рабочим, только когда превращается в вопрос,
который где-то задают.** «Долг, который нельзя погасить работой» — теперь вопрос к каждой записи
реестра: *какой работой эта строка вычёркивается?* Нет ответа — чинить надо не код, а меру.
📜 Трение — сигнал. Механизм, который приходится обслуживать вручную при каждой обычной правке,
почти всегда стоит не на своём месте.

## 2026-09-04 — признак «есть тесты» не видел тесты Go

Прогнали `aqk` на семи скачанных репозиториях разного размера и языка (JS, TS, Python, Go —
суррогат второго пользователя, раз настоящего пока нет). На `gin-gonic/gin` — 40 файлов `*_test.go`
рядом с кодом — `doctor` показывал репозиторий без тестов: признак `has_tests` знал только про
папки `tests/spec/__tests__` и суффиксы `.test.js`/`test_*.py`, взятые с оглядкой на JS и Python,
без проверки на Go.

**Чем это стоило.** Ничем прямо сейчас — признак не гейт, а подсказка триггерам. Но неверный факт
о репозитории тихо меняет, какие записи каталога считаются применимыми.

**Вывод.** 🔧 `detectFacts` в `tool/lib/repo.mjs` теперь распознаёт `*_test.go` как тесты
(проверено `tool/selfcheck/smoke.sh`, красный тест до правки). 📜 Признак, добавленный «под один
язык», нужно перепроверять на каждом новом языке в каталоге — само наличие Go в `EXT_LANG` не
значило, что все признаки про Go знают.

## 2026-09-04 — «внешняя ссылка» опознавалась только по началу строки

Тем же прогоном на gin-gonic/gin: `entry-links-exist` принял ссылку в `CHANGELOG.md` вида
`[#3227](<(https://github.com/.../pull/3227)>)` за битую локальную — фильтр `case "$T" in http*)`
смотрел только на начало цели, а автоссылка была обёрнута в `<(...)>`.

**Чем это стоило.** Ложное падение гейта на честном markdown — то, из-за чего гейты в целом
перестают доверять.

**Вывод.** 🔧 `kit/gates/entry-links-exist/check.sh` теперь ищет `http://`/`https://` где угодно
внутри цели, а не только в начале (проверено `tool/selfcheck/gates.sh`, добавлен зелёный образец).
📜 Фильтр по префиксу ловит только один вариант написания — для внешних ссылок надёжнее спросить
«есть ли протокол внутри», а не «начинается ли строка с протокола».

## 2026-09-04 — `if:` не защищает `env:` того же шага

Первый живой прогон `.github/workflows/triage.yml` (issue #2) провалился дважды подряд.
Во-первых, `--max-turns 1` не дал агенту дописать до `structured_output` — упал с `is_error`.
Во-вторых, следующий шаг «сообщить в Telegram» рухнул отдельной ошибкой шаблона: его `env:`
считал `fromJSON(steps.ask.outputs.structured_output).reason` без защиты от пустой строки — а
GitHub вычисляет `env:` шага ДО того, как решит его пропустить по `if:`. Защита стояла только на
самом `if:`, а не на соседнем `env:`, который читает то же самое поле.

**Вывод.** 🔧 `--max-turns` поднят до 3, `REASON` обёрнут тем же `|| '{}'`, что и `if:` рядом.
📜 В GitHub Actions `if:` одного шага не защищает `env:` того же или следующего шага — каждое
обращение к необязательному выводу нужно страховать отдельно, рядом с использованием, а не один
раз в условии.

## 2026-09-04 — секрет в контакте с недоверенным вводом в собственном конвейере

Готовя релиз, прошёл `kit/rules/security.md` заново глазами постороннего — и он же прямо описывает
то, что в тот же день собрал сам: «агент, читающий недоверенное, работает без секретов в окружении
и без прав на запись». `triage.yml` держит живой `CLAUDE_CODE_OAUTH_TOKEN` (годовой токен подписки)
в окружении агента, который читает текст **любого** issue/PR в публичном репозитории — а `permissions:`
без прав записи защищает только от публикации в GitHub, не от утечки самого токена через инструмент
с выходом в сеть, если такой включён у агента по умолчанию.

**Чем это стоило.** Ничем — поймано до того, как кто-то посторонний успел открыть issue. Но правило
уже было написано словами в собственном своде за несколько часов до того, как его нарушили в коде.

**Вывод.** 🔧 `triage.yml` переведён на `workflow_dispatch` (только ручной запуск), пока не проверен
набор инструментов агента. 📜 Написанное правило не читает себя само — держать рядом с новым
конвейером, который трогает секреты, привычку сверяться с `kit/rules/`, а не полагаться на память
о том, что там написано.

## 2026-09-04 — `deps-are-pinned` требовал go.sum там, где его не бывает

Ручной разбор `kit/gates/*/check.sh` перед релизом. `go.sum` **не создаётся** Go-тулчейном, если
модуль использует только стандартную библиотеку — а гейт требовал его для любого `go.mod` без
разбора, есть в нём зависимости или нет.

**Чем это стоило.** Ничем — поймано до того, как кто-то поставил гейт на настоящий zero-dependency
Go-проект. Но это третий случай одного класса за месяц: «долг, который нельзя погасить работой» —
после `gate-has-samples` (2026-08-24) и `no-print-in-prod` (2026-09-03).

**Вывод.** 🔧 `has_deps_declared` теперь проверяет `go.mod` на `^require` перед тем, как требовать
`go.sum`, — тем же способом, каким уже проверялся `package.json`. Добавлены `red/`/`green/`
образцы на оба случая. 📜 Признак «класс уже случался дважды» стоило превратить в чек-лист раньше:
каждый гейт, требующий сопутствующий файл, нужно спросить «а бывает ли законный случай без него?»
до того, как он попадёт в каталог, а не после третьей находки.

## 2026-09-04 — реальный секрет в пользовательской `red/` был невидим всем гейтам

Самая серьёзная находка сессии. `_skip.sh` прятал образцы каталога (`kit/gates/<имя>/red/`,
`.../green/`) через `--exclude-dir=red`/`-name red -prune` — а эти примитивы matчат только
**имя** папки, не путь целиком. Реальная пользовательская папка `red/` (red-team тесты, что
угодно — имя не выдуманное) молча исключалась из **семи** гейтов сразу: `secrets-not-in-code`,
`no-print-in-prod`, `todo-without-task`, `file-size-limit`, `complexity-limit`, `duplicate-code`,
`swallowed-error`. Проверено прогоном: настоящий AWS-ключ в `<проект>/red/config.py` не находил
`secrets-not-in-code` вовсе — код `0`, тишина.

**Чем это стоило.** Ничем на практике — ни один реальный проект пока не ставил эти гейты на код
с папкой `red/` или `green/`. Но это ровно тот случай, который «Готово = доказано» требует ловить
до находки, а не после: гейт про секреты — последняя линия защиты, и она молчала бы именно там,
где молчать нельзя.

**Вывод.** 🔧 `_skip.sh`: `sample_dirs()` (имя-фильтр) заменён на `own_samples_filter()` —
проверяет путь целиком (`/gates/<имя>/(red|green)/`), а не голое имя каталога. Подключён во всех
семи местах. Красный тест на самом опасном случае (`secrets-not-in-code`) добавлен в `smoke.sh`
постоянно, не разово. 📜 `--exclude-dir`/`-prune` сравнивают по имени, а не по пути — когда нужно
исключить конкретное МЕСТО в дереве, а не любую папку с таким именем, нужен путь-осведомлённый
фильтр после сбора файлов, а не примитив find/grep до него.

## 2026-09-04 — `lesson-has-outcome` не видел ни одну запись этой сессии

Гейт признавал шишкой только раздел со строкой `**Класс:**` и отметкой в цитате (`> ✅ …`) —
формат исходного переноса из `audit_project`. Все записи этой сессии (с 2026-09-03) пишутся
иначе: `## ГГГГ-ГГ-ГГ — …`, вывод абзацем `**Вывод.**` без цитаты. Гейт их не распознавал вовсе —
не «не хватало отметки», а раздел не считался записью, требующей проверки в принципе. Больше
пятнадцати записей этой сессии прошли мимо собственного же арбитра, который должен был их
проверять — зелёный `doctor --run` не доказывал того, что предполагалось.

**Чем это стоило.** Ничем — вручную формат соблюдался каждый раз. Но «вручную соблюдался» и
«машина проверила» — разные утверждения, и весь смысл этого гейта в том, чтобы не путать одно
с другим.

**Вывод.** 🔧 Раздел считается записью, если заголовок начинается с даты (`## ГГГГ-ГГ-ГГ`);
отметка ищется где угодно в разделе, не только в цитате — оба формата (старый и новый)
законны одновременно. Красный тест на записи без вывода добавлен как основа правки, не после.
📜 Проверка, написанная под один формат записи, молча слепнет, когда формат эволюционирует —
её область действия стоит проверять первой строкой у любого «зелёного» результата, который
кажется подозрительно постоянным.

## 2026-09-04 — своя документированная команда «порог для конвейера» не проверяла гейты

Ручное ревью `doctor.mjs`. `--min N` без `--run` никогда не проваливается по вине упавших
гейтов: `gateFailed` инициализирован нулём и меняется только внутри `if (wantRun)`. А `README.md`
и `AGENTS.md` оба рекомендовали именно `doctor --min 1` — **без** `--run`, хотя настоящий
`.github/workflows/ci.yml` всегда использует `--run --min 1`. Проверка есть — предупреждение
«N гейтов объявлено, но не запускалось» действительно печатается, — но итоговая строка «Порог
AQK-1 пройден» выходит зелёной, а именно код возврата, не текст, читает конвейер.

**Чем это стоило.** Ничем в этом репозитории — здесь везде `--run`. Но задокументированная
команда «порог для конвейера» — самая вероятная, которую скопирует в свой CI кто-то посторонний.
Ровно тот класс, ради которого весь стандарт: тишина неотличима от успеха.

**Вывод.** 🔧 `README.md` и `AGENTS.md` поправлены на `doctor --run --min 1`. 👤 Более глубокая
защита — не давать `--min` вообще ничего утверждать без `--run` — оставлена человеку: это меняет
поведение команды, а не только текст документации, и решение не моё.

## 2026-09-04 — заготовка `aqk new` несла ту же дыру, что чинили пять минут назад в каталоге

Продолжение ручного ревью `tool/`. `CHECK_SH_TEMPLATE` — то, что `aqk new` кладёт в свежий
гейт, — использовал `--exclude-dir=red` по голому имени, тот самый паттерн, который только что
чинили сразу в семи записях каталога. Каждый новый гейт, заведённый контрибьютором, наследовал бы
дыру заново. Попутно фикс на `own_samples_filter` сломал заготовку в другую сторону — `_skip.sh`
не копируется в проект при `new` (только при `add`), и `skip_grep`/`own_samples_filter` были
не определены. Обе половины нашлись только прогоном заготовки в чистом проекте, не чтением кода.

**Чем это стоило.** Ничем — контрибьюторов пока нет. Но чинить общий механизм и не проверить
шаблон, которым пользуются все будущие записи, — значит закрыть дыру один раз из двух мест.

**Вывод.** 🔧 `CHECK_SH_TEMPLATE` подключает `_skip.sh` и зовёт `own_samples_filter`, как и
настоящие гейты; `cmdNew` копирует `_skip.sh` в проект, как это уже делал `installGate` для
`add`. Постоянный тест в `smoke.sh`. 📜 Правка общего механизма обязана пройтись и по шаблону,
которым пользуются все будущие записи, — не только по существующим.

## 2026-09-04 — дифф проверяет реализацию, но не замысел

Разбор чужого корпуса практик (методички другого проекта, принесённые владельцем). Независимо
повторяющийся вывод трёх источников: тест сверяет код со спекой, но не может проверить сам
замысел; в одном разобранном проекте 416 из 1069 коммитов за 90 дней оказались `fix` — не
фичей, а стабилизацией уже выкаченного; диагноз владельца того проекта — «долг копится там, где
человека убрали из центра принятия решений».

У нас это уже было словами — «Объяснить дифф до слияния» в `kit/rules/general.md` — но без
машины за этим правилом: агент мог согласиться объяснить, а мог и не согласиться, и никто бы не
заметил разницы по зелёному прогону.

**Вывод.** 🔧 Новый гейт каталога `commit-explains-itself`: последний коммит обязан нести
короткие разделы `Сделано:` / `Не уверен:` в теле. Машина не проверяет, понял ли автор замысел —
только что артефакт-отчёт существует, дальше решает читающий историю человек. 📜 Мягкое правило
без гейта — это пожелание, а не правило; превращать в гейт стоит сразу, как только нашёлся
дешёвый машинный способ проверить хотя бы форму, даже если не содержание.

## 2026-09-04 — первый запуск у постороннего молчит три минуты

**Проект:** aqk

**Что случилось.** Проверка того самого пути, который README предлагает постороннему —
`npx github:arsen-ask-lx/Agent_Quality_Kit doctor` на чистом репозитории. Команда не выдала ни
строчки за 180 секунд; я решил, что она зависла, и перезапустил. Обе попытки в итоге завершились
успешно, кодом 0. Ничего не сломано — просто `npx` клонирует репозиторий целиком, со всеми
методичками и образцами, и до конца клона не печатает ничего.

**Почему это важнее, чем выглядит.** Три минуты тишины — это первое, что видит человек, которому
комплект дали попробовать. У него нет оснований считать, что это нормально: у него есть Ctrl+C.
Я сам, зная устройство, принял это за зависание — посторонний тем более примет. Мы измеряли
корректность установки и ни разу не измеряли, сколько она длится и что видно, пока она идёт.

**Вывод.** 🔧 Выпуск в npm: пакет — 222 КБ и 137 файлов против клона всего репозитория, и
установка из упакованного вида уже проверена целиком (чистый проект → `init` → `add` →
`doctor --run` нашёл настоящий `print`, код возврата 1). 👤 Решение о публикации за владельцем —
имя в реестре занимается навсегда и требует его учётной записи.
