// Strings of the HTML report for a human (`tool/lib/report-html.mjs`). Kept in its own file for
// the same reason as `en-docs` and `en-gates`: the main catalog has its own size limit.
//
// The format is the owner's decision of 2026-09-26: "this step" first, then "done", "debt",
// "ahead", each with a short explanation in bold labels and a checklist. Plain words, no lost detail.
export const enReport = {
  html: {
    lang: "en",
    title: "AQK report",
    eyebrow: (name) => `aqk report · ${name}`,
    meta: (when, head, version, n, secs) =>
      [when, head && `commit ${head}`, version && `aqk ${version}`, `${n} checks · ${secs} s total`].filter(Boolean),
    headline: { red: (n) => `Red checks: ${n}`, cannot: (n) => `Could not check: ${n}`, clean: "Everything declared is green", none: "No run yet" },
    ofGreen: (ok, n) => `${ok} of ${n}`,
    noRunHint: (s) => `run \`${s} doctor --run\``,
    step: {
      title: "This step",
      made: "Done:",
      bad: "Wrong:",
      next: "Next:",
      run: (when, ok, n) => `run ${when}: ${ok} of ${n} checks passed.`,
      first: "nothing to compare with — this is the first full run in history.",
      same: "since the previous full run nothing broke and nothing was fixed.",
      nothingBad: "nothing failed.",
      bad1: (red, cannot) => [red && `${red} findings`, cannot && `${cannot} could not check`].filter(Boolean).join(", ") + " — listed under \"Debt\".",
      nextNone: "no computed steps.",
    },
    done: {
      title: "Done",
      short: "In short:",
      gives: "What it gives you:",
      shortText: (ok, n, level) => `${ok} of ${n} checks passed${level >= 0 ? `, level AQK-${level}` : ""}.`,
      givesText: "every line below is a promise of the project held by a machine, not by the agent's good will.",
      none: "No checks passed.",
    },
    debt: {
      title: "Debt",
      short: "In short:",
      risk: "What it risks:",
      shortText: (n) => `${n} places do not fully work.`,
      riskText: "here the project may look better protected than it is: the agent slips past and nobody notices.",
      none: "No debt — everything declared works.",
      fail: "finding: the check went red, the code breaks the promise",
      cannot: "could not check: the tool is broken, not the code",
      probeNever: (s) => `whether the checks catch a real defect has not been tested; \`${s} probe\``,
      probeOff: "the probe is turned off in the manifest: nobody will learn whether the checks catch defects",
      probeUnknown: "probe state unknown",
      probeStale: (n, s) => `the probe is ${n} commits behind — its answer describes old code; \`${s} probe\``,
      probeBlind: (n) => `${n === 1 ? "defect class" : `${n} defect classes`} caught by no check:`,
      human: (h, t, entry) => `${h} of ${t} rules in ${entry} are held only by a human — the machine does not check them`,
      ratchet: (name, n) => `debt under the "${name}" ratchet: ${n} — may only go down`,
    },
    ahead: {
      title: "Ahead",
      short: "In short:",
      why: "Why this order:",
      shortText: "what to install or fix next.",
      whyText: "first what the project already has, then what the probe found uncaught, then a start from the catalog.",
      more: (n) => `And ${n} more — the full list is in the doctor output.`,
      none: "No computed steps.",
      hint: "These lines can be handed to the agent word for word.",
    },
    trend: {
      gone: "left the run",
      broke: "broke",
      fixed: "fixed",
    },
    grid: {
      title: (runs, n, red) => red ? `${runs} runs, ${n} checks: red ${red} times` : `${runs} runs, ${n} checks: none went red`,
      sub: "row — a check, column — a run · problems on top",
      label: "Checks by run",
      ok: "passed",
      fail: "finding",
      cannot: "could not check",
      none: "not run",
      empty: "At least one run is needed.",
    },
    copy: "Copy",
    copied: "Copied",
    footer: (s) => `Built by ${s} report --html from .aqk/history.jsonl, .aqk/last-run.md and .aqk/last-probe.md. One file, no server, no internet.`,
    written: (p) => `report for a human: ${p}`,
  },
};
