// Strings of the HTML report for a human (`tool/lib/report-html.mjs`). Kept in its own file for
// the same reason as `en-docs` and `en-gates`: the main catalog has its own size limit.
export const enReport = {
  html: {
    lang: "en",
    title: "AQK report",
    eyebrow: (name) => `aqk report · ${name}`,
    meta: (when, head, version, n, secs) =>
      [when, head && `commit ${head}`, version && `aqk ${version}`, `${n} checks · ${secs} s total`].filter(Boolean),
    headline: { red: (n) => `Red checks: ${n}`, cannot: (n) => `Could not check: ${n}`, clean: "Everything declared is green", none: "No run yet" },
    q1: "What is protected?",
    q2: "Does the protection actually catch defects?",
    q3: "Better or worse than last time?",
    ofGreen: (ok, n) => `${ok} of ${n}`,
    greenWord: "checks green",
    levelPill: (l) => (l < 0 ? "no level reached" : `level AQK-${l} by the last run`),
    noRun: "no run yet",
    noRunHint: (s) => `run \`${s} doctor --run\``,
    probe: {
      never: ["—", "the probe has not been run", "unk"],
      off: ["—", "the probe is turned off in the manifest", "unk"],
      unknown: ["?", "probe state unknown", "unk"],
      blind: (n) => [String(n), n === 1 ? "defect class caught by nothing" : "defect classes caught by nothing", "fail"],
      clean: ["0", "defect classes caught by nothing", "ok"],
      stale: (n) => `the probe is ${n} commits behind`,
      fresh: "the probe is fresh",
      counts: (c, u) => `caught ${c}, could not check ${u}`,
    },
    trend: {
      none: ["—", "nothing to compare with", "history starts with this run"],
      same: ["=", "no change", "since the previous full run"],
      better: (n) => [`−${n}`, "fewer red checks", "since the previous full run"],
      worse: (n) => [`+${n}`, "more red checks", "since the previous full run"],
      shrunk: (n) => [`−${n}`, n === 1 ? "check left the run" : "checks left the run", "not comparable"],
      gone: "left the run",
      broke: "broke",
      fixed: "fixed",
      lingering: "long-standing",
      since: (day, runs) => `since ${day}, runs in a row: ${runs}`,
    },
    s1: "What is protected",
    s2: "Does the protection really catch defects",
    s3: "Better or worse",
    s4: "What to hand to the agent",
    gateState: { ok: "green", fail: "finding", cannot: "could not check", skipped: "skipped" },
    humanRules: (h, t, entry) => `${h} of ${t} rules in ${entry} are held only by a human. The machine does not check them.`,
    probeBlindList: "Caught by nothing:",
    probeWhat: "AQK plants known defects into a copy of the files fixed most often in history and runs your checks. This says what went unnoticed.",
    probeRepeat: (s) => `Repeat: \`${s} probe\``,
    chartLabel: "Red and failed-to-run checks per run",
    legend: { fail: "finding", cannot: "check did not run", ok: "all green", partial: "hatched — a narrowed run, not comparable with a full one" },
    chartEmpty: "At least two runs are needed to show a change.",
    nextNone: "No computed steps.",
    nextMore: (n) => `And ${n} more — the full list is in the doctor output.`,
    nextHint: "These lines can be handed to the agent word for word.",
    copy: "Copy",
    copied: "Copied",
    footer: (s) => `Built by ${s} report --html from .aqk/history.jsonl, .aqk/last-run.md and .aqk/last-probe.md. One file, no server, no internet.`,
    written: (p) => `report for a human: ${p}`,
  },
};
