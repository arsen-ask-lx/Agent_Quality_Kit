# Журнал изменений

Формат — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/). Версии — [SemVer](https://semver.org/lang/ru/).

## [Не выпущено]

### Изменено

- **У приёмки записей появился строгий режим.** `AQK_GATES_STRICT=1` превращает «запись не
  проверена, нет инструмента» из жёлтой отметки в ошибку. Мягкость законна на чужой машине —
  требовать `ruff` от каждого, кто склонировал репозиторий, значит закрыть вклад; но на машине,
  которая инструменты сама и ставит, тильда неотличима от успеха. Найдено разбором зелёного:
  шаг `pipx install vulture` стоял в конвейере с самого его появления, ставил инструмент в
  каталог вне `PATH` задания, и запись `dead-code` **не проверялась конвейером ни разу**.
  В конвейере режим поднят, на Windows и у человека — нет

- **`ci-actually-fails` научен законному укладу.** `continue-on-error` иногда стоит не ради
  прощения провала, а чтобы дать выполниться шагам ПОСЛЕ проверки; вердикт выносит отдельный
  шаг `if: steps.<id>.outcome == 'failure'` → `exit 1`. Так устроен `pre-commit.yml` в
  `fastapi`. Такой шаг больше не находка — но только когда в файле есть И ссылка на исход,
  И падение: переспросить исход и ничего с ним не сделать значит простить провал длиннее на
  три строки. Дефект найден **разбором молчания**: гейт на `fastapi` молчал верно, но по
  неверной причине — слово-примета не совпала с именем шага, а про переспрос исхода он не знал

- **`deps-are-pinned` засчитывает `requirements.txt`.** Закрепляют не только poetry и uv: у
  `httpx` версии инструментов закреплены до патча прямо в `requirements.txt`, а гейт требовал
  ещё и `poetry.lock`, которого в этом укладе не бывает. Файл засчитывается только если сам
  проходит проверку — иначе пустой `requirements.txt` стал бы способом обойти гейт

- **`entry-links-exist` не считает битым адрес страницы сайта.** Путь, кончающийся косой
  чертой (`[руководство](tutorial/#install)`), — адрес на опубликованном сайте, а не файл в
  репозитории; так ссылаются mkdocs, docusaurus и jekyll. Найдено на `fastapi`

- **Ревизия каталога замером: две записи стали чистой делегацией.** `no-print-in-prod` и
  `swallowed-error` лишились переносимых проверок. Прогон по пяти чужим репозиториям пяти
  стеков (`httpx`, `fastapi`, `zod`, `cobra`, `ripgrep`) дал у первой **434 находки и ноль
  настоящих** — бенчмарки, примеры в документации, тесты и печать внутри строки документации;
  у второй настоящим оказался только класс пустых `catch {}`, а питоновский
  `except ImportError: pass` и растовый `let _ = …?` были ложными. Теперь обе записи вызывают
  готовый инструмент (`ruff`, `eslint`, `errcheck`), триггер сужен до языков, под которые он
  есть, а образцы проверяются через `samples_for`. Цена названа вслух: проект на Go или Rust
  по этим пунктам не получает ничего. `ruff` добавлен в конвейер — иначе образцы этих двух
  записей не проверялись бы нигде

- **`complexity-limit` больше не считает тесты.** Замер по `fastapi`: 303 находки, из них 101
  в `tests/`. Глубокая вложенность в тесте — обход таблицы ожиданий, а не сложная логика;
  после исключения осталось 10. Тот же список исключений, что у `duplicate-code`

- **`duplicate-code` перестал считать дублем одинаковые импорты.** Окно, в котором нет ни одной
  строки кроме ввоза и одиноких скобок, находкой не считается: в Go, Java и Rust список ввоза
  идёт по строке на строку и в соседних файлах одного пакета совпадает всегда. Найдено на
  `cobra`. У записи появился рецепт под Python (`pylint --enable=R0801`) — его не было вовсе,
  и питоновский проект получал переносимую самоделку вместо родного инструмента

### Добавлено

- **Запись `no-phantom-package`.** Имена npm-пакетов, названные в документации, сверяются с
  реестром: агент, не знающий инструмента, придумывает правдоподобное имя, следующий агент
  выполняет по нему установку, а занять свободное имя может кто угодно (USENIX Security 2025,
  arxiv 2406.10279: около 20% сгенерированного кода ссылается на несуществующие пакеты).
  Делегирует [`slopcheck`](https://github.com/mattschaller/slopcheck) — MIT, ноль зависимостей;
  объявлен полем `requires`. Обёртка добавляет три вещи, которых у него нет: обход, уважающий
  `own_samples_filter`; отдельный код 2 на «реестр недоступен» (сам инструмент выходит с нулём,
  и тишина неотличима от «чисто»); тот же код, если формат вывода сменился. Красный образец
  выбран так, что его нельзя погасить снаружи: `npm` запрещает заглавные буквы в именах новых
  пакетов, и занять такое имя не может никто

- **`aqk prove` — доказательство гейтов, и ступень AQK-2 теперь его требует.** До этого уровень
  считался наличием файлов, и проект с гейтами `true` — командой, которая всегда отвечает
  «ок», — проходил порог **AQK-3** и получал значок. Проверено прогоном на пустой папке: три
  «гейта», ноль защиты, высший уровень. При том что ступень так и называлась — «гейты
  доказаны». Теперь `prove` гоняет каждый объявленный гейт по его красному и зелёному образцу;
  AQK-2 требует, чтобы ни один доказуемый гейт не был сломан и был доказан хотя бы один.
  `doctor` без `--run` показывает ступени со второй как «не доказано» — узнать это без прогона
  нельзя; значок и отчёт доказывают всегда

- **Запись `test-not-adjusted` — арбитра не правит тот, кто чинит код.** Приём назван
  практиками поимённо: «test subversion: changing tests to pass broken code» в разборе 1154
  обсуждений с Reddit и Hacker News (arxiv 2603.27249). Наше собственное правило требовало
  этого с самого начала и сторожа не имело. Запись **целиком делегирует**
  [`checkwash`](https://github.com/taipei49314/checkwash) — он делает это лучше, чем сделали бы
  мы. Что добавляем мы: измеренный отбор по правилу. На образце «код сломан, утверждения
  заменены на `is not None`» инструмент находит всё и завершается **кодом 0**, понижая находки
  до `warn`; общий подъём порога даёт 15% заблокированных коммитов на здоровой истории `httpx`,
  а отбор по правилу — 2.5%, и та блокировка настоящая

- **Поле `requires` в записи каталога.** Программа, без которой запись не работает, называется
  явно: по первому слову команды этого не видно, когда переносимый рецепт — обёртка вокруг
  готового инструмента. `add` отказывает с причиной, приёмка и мутационная проверка пропускают
  запись со словами «нужен …», строгий режим в конвейере делает такой пропуск ошибкой

- **Запись `personal-config-not-shared`.** `CLAUDE.local.md` и `.claude/settings.local.json`
  по устройству личные, и документация прямо велит их не коммитить. Попав в git, они
  становятся обязательными для всех: личный свод дописывается **после** общего и потому
  перекрывает его, а личные разрешения достаются каждому, кто склонировал репозиторий. Замер
  по тринадцати чужим репозиториям: шесть настоящих находок в пяти, ноль ложных на семи
  контрольных. У `AgentLint` такая проверка есть — живой прогон на репозитории с обоими
  файлами в git дал **61/100 и код возврата 0**: это оценка, а не гейт

- **Признак `has_agent_entry`** — агента здесь используют: есть `CLAUDE.md`, `AGENTS.md`,
  каталог `.claude`, `.cursor/rules` или инструкции copilot. Шире, чем `has_agent_config`:
  настройки заводят не все, а свод — почти каждый

- **Запись `hook-actually-fires` — первая про обвес самого агента.** Хук с неверным именем
  события Claude Code не вызывает и об этом не сообщает: настройка выглядит как защита и
  защитой не является. Замер по 48 чужим `.claude/settings.json` с GitHub: **ноль ложных на 39
  случайных и шесть настоящих находок в четырёх репозиториях** — `PreToolCall`, `PostToolCall`,
  `preToolCall`, среди них `typecheck && test` перед коммитом, не запускавшийся ни разу. Вторая
  половина записи — `matcher` на событии, которое его не поддерживает: документация дословно
  называет такой `matcher` молча игнорируемым, и автор думает, что отобрал, а хук срабатывает
  на всём. Список из 33 событий снят с первоисточника, дата стоит в самой проверке

- **Признак `has_agent_config`.** Записи про настройки агента не показываются проектам, где
  агента не настраивали: `.claude/settings.json`, `.claude/settings.local.json`,
  `.claude/hooks.json`, `.claude/hooks/hooks.json`

- **Совет по починке виден всегда.** Все записи каталога печатали «почини: …» последней строкой,
  а прогон показывал три первые и обрезал остальное — то есть ровно ту строку, ради которой
  человек смотрит на красное, он не видел никогда. Теперь находки обрезаются, совет — нет.
  Строка стала требованием приёмки: пятый факт в списке `SPEC.md` §6, для записей с переносимым
  рецептом. Запись, делегирующая готовому инструменту, печатает вывод этого инструмента —
  требовать от чужого вывода нашу строку значит требовать невозможного

- **У долга появились цель и срок.** `# aqk-goal: N` в шапке реестра — сколько нарушений
  считать погашенным долгом; по достижении храповик говорит убрать обёртку, потому что реестр,
  который никто не снимает, остаётся навсегда и пропускает нарушения, которых уже нет.
  `# aqk-deadline: ГГГГ-ММ-ДД` — после этой даты непогашенный долг красит гейт даже без новых
  нарушений. Срок без последствия не срок; перенос законен и виден в дифе. `SPEC.md` §7,
  пункты 5 и 6
- **Зелёный гейт тоже бывает разговорчивым.** Вывод успешной проверки не показывался вовсе, и
  сообщение «долг погашен, убери обёртку» уходило в никуда. Теперь у зелёного показываются
  строки с меткой совета — остальной вывод успешной проверки остаётся шумом

- **Каталог строк разделён по смыслу.** Он перерос собственный предел в 500 строк, и поймал это
  наш же гейт `file-size-limit` на прогоне. Шов не пополам: в `ru.mjs`/`en.mjs` осталось то, что
  программа **говорит** в терминал, в `ru-docs.mjs`/`en-docs.mjs` уехало то, что она **пишет** в
  файлы — комментарии манифеста, названия ступеней, форма отчёта. У этих текстов разная жизнь:
  первый человек читает секунду, второй — месяцы спустя в своём репозитории

- **Совещательные гейты — третий способ ввести правило.** `advisory:` в манифесте перечисляет
  гейты, которые показывают находки, но не роняют прогон: когда правило ещё обсуждают, а выбор
  «включить и сломать сборку либо не включать вовсе» — не выбор. Объявлением, а не флагом
  прогона: флаг понижает всё разом, не виден в дифе и не назван в итоге — это `continue-on-error`,
  который мы сами и красим. Список печатается **каждый** прогон: совещательный гейт, о котором
  забыли, — это выключенная проверка. `SPEC.md` §7a

- **Правила переносятся в проект на языке вывода.** `init` клал русский текст в любой проект —
  на англоязычной площадке это первое, что человек открывает, и первое, чего он не читает.
  Добавлен `kit/rules-en/`; русская версия остаётся источником истины. Совпадение набора файлов
  сторожит модульная проверка, совпадение содержания не сторожит никто — и это названо в
  `AGENTS.md`, а не спрятано

### Исправлено

- **Десять проверок каталога объявляли проект здоровым, потеряв свою библиотеку.** Строка
  `. …/_skip.sh 2>/dev/null || SKIP_NAMES=…` выглядела страховкой и ею не была. Под `sh` (dash)
  неудачный `.` завершает скрипт немедленно, и ветка после `||` не выполняется никогда. Под
  `bash`, которым гейты и запускаются из манифеста, она выполняется — но подставляет только
  ПЕРЕМЕННУЮ: функции обхода остаются неопределёнными, конвейер печатает пустоту, проверка
  выходит с нулём. Замер: одинокая копия `todo-without-task` на файле с `# TODO: доделать потом`
  сказала «чисто». Теперь существование файла проверяется до `.`, отсутствие даёт код 2 и
  причину. Приёмка каталога сторожит это прибором: запускает одинокую копию каждой переносимой
  проверки и требует ненулевого кода

- **Упавший гейт больше не стирает реестр долга.** Провал без единой разобранной находки — это
  отказ инструмента, а не чистый прогон: пустой список означает «не знаем», а не «чисто».
  Раньше такой прогон вычёркивал весь реестр и возвращал ноль, а с появлением цели вдобавок
  предлагал снять защиту — по итогам прогона, которого не было
- **`--since` больше не отменяет срок долга.** Сообщение храповика называет путь к реестру,
  реестра в дифе нет — строка отбрасывалась, и гейт, обязанный краснеть по сроку, печатался
  зелёным. Совет теперь отделяется от находок ДО сужения, а гейт без находок дифом не сужается
- **Совет, напечатанный посреди находок, больше не проглатывает их.** Собирается построчно, а не
  хвостом от первой метки: гейт, зовущий помощник шесть раз, давал сорок строк без обрезки
- **Опечатка в `aqk-goal` или `aqk-deadline` называется вслух.** Нечисловая цель молча отключала
  сравнение, дата вида `2026/12/31` не наступала никогда — а `2020/01/01` наступала, и поведение
  выглядело случайным, а не отсутствующим

## [0.6.0] — 2026-09-06

### Добавлено

- **`doctor --run --since <ссылка>` — только то, что внёс диф.** Первый прогон в живом проекте
  показывал долг за все годы: стену красного не разбирают, проверку выключают. Теперь находки
  сужаются до файлов, которых касался диф. Три исхода названы вслух: находка внутри дифа —
  красный; находки только снаружи — зелёный **с числом спрятанного**; гейт, печатающий вердикт
  без путей, сузить нечем — он остаётся красным, и причина названа. Молчаливого «всё хорошо»
  здесь нет ни в одном из трёх случаев
- **Запись каталога `gate-not-weakened`** — первая, ловящая приём, а не стиль: не «код плохой»,
  а «сигнал о плохом коде выключен». Красным делается безадресное подавление — `# noqa` без кода,
  `eslint-disable` без имени правила, `@ts-ignore`, `@SuppressWarnings("all")`, `--no-verify` в
  конвейере. Проверено замером по пятнадцати чужим репозиториям: 20 774 файла, 22 находки,
  19 настоящих. Замер же нашёл два дефекта в самой проверке — оба до внесения записи в каталог
- **Запись каталога `ci-actually-fails`** — закрывает дыру, найденную в собственной оснастке:
  `gates-run-in-ci` проверял, что гейт **упомянут** в конфиге конвейера, и пропускал
  `run: pytest || true` зелёным. Новая запись красит шаг, который и выносит вердикт, и не может
  провалиться. Замер по пятнадцати чужим репозиториям: 2 находки, обе настоящие (у `reviewdog`
  два шага собственных линтеров под `continue-on-error: true`), 0 ложных — после трёх
  переписываний, каждое из которых потребовал замер
- **Запись каталога `test-has-assertion`** — три случая, в которых тест зелёный при любых
  данных: пустое тело, утверждение-тавтология, пропуск без названной причины. Граница найдена
  замером: первая формулировка «тест без утверждения» дала 1843 находки, из них 1843 ложных —
  186 оказались законным стилем «вызвали и проверили, что не упало». После шести переписываний:
  8 находок на ~25 000 файлов в девятнадцати репозиториях, включая шесть выключенных без причины
  тестов в `swr`
- **Запись каталога `promise-has-gate`** — центральный вопрос комплекта: у каждого правила в
  точке входа виден сторож. Правило несёт пометку `<!-- aqk: имя-гейта -->` или
  `<!-- aqk: человек -->`; пометка, ведущая в несуществующий гейт, — тоже красное. Первый прогон
  по собственному `AGENTS.md`: тринадцать железных правил, ни у одного сторож не назван. После
  разметки одиннадцать из тринадцати оказались исполняемыми человеком — и это стало видно
- **Шаблон `AGENTS.md`, который кладёт `init`, размечен** на обоих языках: у каждого правила
  в чужом проекте сразу виден сторож
- **Раздел «обвес самого агента» в карте готовых правил** — `agnix` (455 правил, активен) и
  `agents-lint`. Запись `docs-commands-run` **отменена, не начата**: `agents-lint` делает это
  лучше. Поиск перед работой снял с плана целую запись
- **Скорость проверок каталога.** Список файлов больше не накапливается строкой в цикле
  оболочки — это квадрат по длине: на 6407 файлах проверка шла 44 секунды, не прочитав ни одного
  файла. Отбор делает `find`, признак «сгенерирован» — сам awk. Стало 1,1 с и 2,3 с
- **Раздел «заглушка вместо реализации» в карте готовых правил.** Запись `no-stub-in-done`
  **написана, измерена и не внесена**: из 120 находок 91 оказалась законным приёмом
  (null-объекты, безопасные заглушки), 29 покрывает pylint W0223, своей доли на 25 000 файлах не
  осталось. Готовые правила — `no-empty-function`, `W0223`, `F901`, `PIE790` — внесены в карту
- **Правило «сомнение — повод посмотреть наружу»** — железным правилом в `AGENTS.md`, разделом
  в `kit/rules/general.md` (переносится в проекты) и навыком `.claude/skills/search-first`

- **Зрелость записи каталога считается машиной.** Три состояния: `stable` — доказательство
  ссылается на журнал шишек, `experimental` — не ссылается, `deprecated` — объявлено вместе с
  преемником. Объявить себя зрелым нельзя: `lifecycle: stable` в записи отклоняется приёмкой.
  У всех трёх соседей, чей каталог мы разбирали, это поле заполняет автор — значение, написанное
  автором, означает доверие к автору, а не факт. `PROJECT.md` §9 п.4 закрыт
- **`aqk add` отказывает в выведенной записи** и называет ту, что её заменяет. Запись, которую
  всё ещё можно поставить одной командой, не выведена, а просто помечена. `aqk start` такую
  запись пропускает, называя преемника в общем списке пропущенного
- **`tool/selfcheck/lifecycle.mjs`** — таблица зрелости всего каталога одной командой. Правило
  живёт в одной функции (`entryLifecycle`) и оттуда же читается приёмкой: повтор на sh дал бы
  второй источник истины, расходящийся с первым молча

## [0.5.0] — 2026-09-06

### Добавлено

- **Конвейер прогоняет комплект на Windows.** Задание `windows-latest` рядом с `ubuntu-latest`,
  оболочка — Git Bash. `PROJECT.md` §9 п.2 закрыт настоящим арбитром: «работает на Windows»
  перестало быть рассуждением. Первый же прогон нашёл шесть дефектов, три из них в самой программе
- **Мутационная проверка записей** — `tool/selfcheck/mutation.sh`, объявлена гейтом. Образец
  меняется так, что вердикт меняться НЕ обязан (сдвиг строк, windows-переносы), и вердикт обязан
  не измениться. Пара образцов доказывает одно срабатывание, мутации доказывают класс. §9 п.4
  закрыт; первый прогон нашёл четыре записи из пятнадцати, ломавшихся на CRLF
- **`doctor --baseline`** — четырнадцать пунктов методички «обязательный минимум проекта» из
  пятидесяти подтверждаются прогоном, и вывод называет, чем именно: файл в корне, объявленный
  гейт, осмотр репозитория, поле манифеста или зависимость. Остальные тридцать шесть названы
  числом, а не спрятаны. Каждый признак — семейство маркеров разных экосистем: файл-замок
  засчитывается по `Cargo.lock`, `poetry.lock`, `go.sum`, `mix.lock` наравне с `package-lock.json`
- **Три хука для экосистемы pre-commit** (`.pre-commit-hooks.yaml`): `aqk` — блокирующий,
  `aqk-doctor` — только осмотр, `aqk-baseline`. `pre-commit` ставит пакет сам; зависимостей нет
- **Запись каталога `color-from-token`** — цвет приходит из токена темы, а не литералом в
  компоненте. Первая запись, перенесённая из другого проекта. Ложных срабатываний на живом
  проекте: ноль
- **Признак осмотра `has_ui`** и условие триггера по нему: запись про внешний вид больше не
  показывается бэкендам, библиотекам и утилитам командной строки
- **`llms.txt`** — машиночитаемая выжимка в корне по соглашению llmstxt.org
- **Железное правило «Прибор раньше улучшений»** — прежде чем чинить, заводи то, чем видно, что
  чинить. Доказательство внутри правила: мутационная проверка нашла четыре записи, которых не
  было в списке того, что мы собирались чинить

### Исправлено

- **Порог перестал спорить сам с собой.** При упавшем гейте печаталось «Порог AQK-1 НЕ пройден:
  сейчас AQK-1». Теперь развилки две, и вторая называет упавший гейт поимённо
- **Ключ храповика больше не зависит от машины.** `duplicate-code` печатал пару файлов в порядке
  обхода `find`, а он свой на каждой системе: реестр с одной машины краснел целиком, а храповик
  те же дубли вычёркивал как исправленные — то есть портил данные
- **Родной инструмент перестал читать то, чего не читают переносимые проверки.** Первой находкой
  `duplicate-code` на живом проекте были методички самого комплекта в `.aqk/`. Замер: 5597 → 2958
  строк вывода, находок не по коду 435 → 0
- **Решётка в команде больше не обрезает команду молча.** Разбор манифеста резал строку по `#`
  безусловно: гейт запускал не то, что объявлено
- **Опечатка в поле манифеста называется вслух**, а не читается как «поля нет»
- **`start` не бросает установку из-за одной записи.** На машине без нужного инструмента ставилось
  три сторожа из двенадцати, и про остальные девять не говорилось ничего
- **Разделитель платформы не утекает в документы.** На Windows подпись в `GOD_AI.md` выходила
  `kit\docs`, а отчёт печатал `.aqk\docs\ai\...`
- **Приёмка каталога не отклоняет всё подряд на Windows** — проверка разделов README искала
  через `grep -i` и не находила ни одного
- **Гейт про мини-отчёт не краснеет на слитом предложении изменений** — узнаёт слияние по форме
  (два родителя без разделов отчёта), а не по словам

### Безопасность

- **`SECURITY.md`: модель доверия.** Записано, что `.aqk.yml` — исполняемое содержимое, и назван
  выбор для репозитория, принимающего чужие изменения: считать порог по доверенной ветке

## [0.4.2] — 2026-09-04

### Изменено

- **Описание пакета и репозитория — по-английски.** README перевели в 0.2.5, а две строки,
  которые человек видит РАНЬШЕ любого README, остались русскими: описание в выдаче поиска npm
  и подпись репозитория на GitHub. Кириллица в результатах поиска закрывает страницу до того,
  как её открыли, — то самое рассуждение, ради которого README и переводили
- Ключевые слова пакета и темы репозитория: добавлены `agents-md`, `agents`, `github-action`,
  `npx`. Убрана тема `claude-code-plugin` — комплект плагином не является, а тема утверждала
  обратное

## [0.4.1] — 2026-09-04

### Добавлено

- **Действие для GitHub Actions** — `action.yml` в корне: поставить проверки в чужой конвейер
  тремя строками вместо запоминания команды. Своей логики не имеет намеренно — обёртка над
  `doctor --run`, иначе конвейер однажды начал бы проверять не то, что проверяет программа.
  Отдельное задание в нашем конвейере запускает это действие на чистом подопытном проекте
  при каждом пуше: оно ставит выпущенный в реестр пакет, то есть ломается там, где обычный
  прогон по нашему коду ничего бы не заметил

### Исправлено

- `aqk badge` печатал «1 gates» и «гейтов 1, все зелёные» — согласование по числу ломалось
  ровно в том случае, который увидит новый пользователь: один поставленный гейт. Формулировка
  переписана так, что не зависит от числа
- В таблице команд `AGENTS.md`, которую `init` кладёт в проект, не было `aqk badge` — агент,
  которому велели показать уровень, о команде не узнавал

## [0.4.0] — 2026-09-04

### Добавлено

- **`aqk badge` — значок уровня, который нельзя нарисовать.** Строка для README печатается
  только после прогона объявленных гейтов; при красном гейте не печатается ничего — значок при
  красном гейте есть заявление автора, а не факт машины. `aqk badge --check` роняет конвейер в
  тот день, когда значок в README разошёлся с прогоном. Обычный значок соответствия рисуют один
  раз, и через месяц он врёт молча; здесь враньё стоит красного конвейера. Наш собственный
  значок AQK-3 проверяется этой командой при каждом пуше

### Изменено

- В обоих README сказано прямо: `doctor` только читает — не пишет ни одного файла и никуда
  ничего не отправляет. Первый вопрос к незнакомому инструменту, которому предлагают показать
  свой репозиторий, именно этот, и ответ на него не должен требовать чтения исходников

## [0.3.0] — 2026-09-04

Первый прогон в чужом проекте (Django, Windows) нашёл четыре дефекта. Все закрыты.

### Исправлено — молчаливая деградация

- **На Windows все родные рецепты были недостижимы.** Наличие программы проверялось командой
  `command -v` через оболочку, а там оболочка — `cmd.exe`, где такой команды нет вовсе: ответ
  «не установлено» приходил для любой программы. Гейт молча вставал на слабейший переносимый
  вариант, а `doctor --run` показывал зелёное. Теперь свой обход PATH с учётом PATHEXT
- `aqk note` игнорировал `lessons:` из манифеста проекта и требовал клон нашего репозитория,
  чтобы записать урок туда. Теперь сначала журнал этого проекта

### Добавлено

- **`aqk report` — обязательная форма отчёта**, собранная прогоном, а не памятью: что держит
  машина и каким рецептом (родным или переносимым), что стоит слабее возможного, чего нет,
  что комплект велел прочитать, и коротко зачем. Красный гейт даёт код возврата 1.
  Форма вписана железным правилом в `AGENTS.md`, который `init` кладёт агенту
- **`.aqkignore`** — исключения самого проекта для кода, который лежит здесь, но написан не
  здесь. Читается общим `_skip.sh`, поэтому действует сразу на все сканирующие проверки.
  Раньше единственным способом была правка копии `_skip.sh`, которую затирает `aqk add`
- **Два языка.** Весь вывод программы, намерения записей каталога и файлы, которые `init`
  кладёт в проект (`AGENTS.md`, `CLAUDE.md`, манифест, заготовка гейта) — на русском и
  английском. Язык: `AQK_LANG`, иначе системная локаль, иначе английский. Что каталоги строк
  не разошлись, сторожит модульная проверка

### Изменено

- Из `no-print-in-prod` убрано жёсткое `--exclude-dir=gates-reference` — имя каталога одного
  проекта, зашитое в общий инструмент. Такому место в `.aqkignore` самого проекта

## [0.2.5] — 2026-09-04

### Добавлено

- `README.md` по-английски, `README.ru.md` по-русски, переключатель языка в начале обоих. npm и
  GitHub показывают английский по умолчанию: страница на кириллице закрывается за две секунды
  теми, кто мог бы этим пользоваться

### Известное ограничение

- Переведена только дверь. Вывод программы, правила в `kit/rules/` и описания записей каталога
  остаются русскими — человек, пришедший по английскому README, увидит кириллицу в терминале на
  первой же команде. Это осознанное промежуточное состояние, а не недосмотр: см. журнал шишек,
  «2026-09-04 — перевели дверь, дом остался прежним»

## [0.2.4] — 2026-09-04

Содержит всё, что числилось за 0.2.3: **та версия в реестр не попала** — конвейер выпуска упал
на подъёме npm, а номер под меткой уже был занят. Ставьте 0.2.4.

### Исправлено

- Конвейер выпуска ставил `npm@latest`, то есть уже 12.x, которая требует Node 22+ и падала на
  Node 20. Версия закреплена на `npm@11`: доверенного издателя она умеет, на Node 20 работает

### Добавлено

- Выпуск идёт от метки через доверенного издателя (OIDC): ни токена, ни второго фактора на
  машине, право публиковать выдаётся одному файлу `publish.yml` на один прогон, пакет уезжает с
  удостоверением происхождения (`--provenance`)
- Значки версии, состояния проверок и лицензии в начале README
- Ключевые слова пакета: те, по которым такое ищут (`agentic`, `code-quality`, `ci` и другие)

## [0.2.3] — 2026-09-04 — в реестр не попала

### Добавлено

- Комплект опубликован в npm: `npx agent-quality-kit doctor`. Пакет — 230 КБ вместо клона всего
  репозитория, поэтому первая команда отвечает сразу, а не после двух-трёх минут тишины
- Выпуск идёт от метки `v*` через доверенного издателя npm (OIDC): `.github/workflows/publish.yml`.
  На машине не хранится ни токена, ни второго фактора — право публиковать выдаётся на один прогон
  и только этому файлу. Пакет уезжает с `--provenance`: на странице npm видно, из какого коммита
  и какого прогона он собран. Повод — токен с правом записи, утёкший в переписку в день выпуска;
  отозван в тот же час, но само хранение токена и было слабым местом

### Изменено

- Программа печатает в подсказках короткую форму вызова, а не адрес репозитория: подсказку
  копируют и выполняют, и она должна быть той, которой пользуются
- `repository.url`, `homepage`, `bugs` в `package.json` — npm поправлял первое сам при каждой
  публикации, остальных не было вовсе

## [0.2.2] — 2026-09-04

### Исправлено

- `commit-explains-itself` врал о составе коммита в мелком клоне. `actions/checkout` по умолчанию
  берёт один коммит; у него не видно родителя, и git выдаёт всё дерево как изменённое — поэтому
  в конвейере гейт краснел на каждой записи журнала, хотя локально был зелёным. Теперь мелкий
  клон распознаётся: гейт пропускает себя, печатая причину и способ починки (`fetch-depth: 2`)

### Замечено

- Запись `dead-code` объявлена как `npx --yes knip@6` и тянет инструмент из сети в момент
  прогона: скачивание попадало в пятиминутный предел на гейт и роняло конвейер два раза из
  четырёх при неизменном коде. Лечится шагом прогрева перед прогоном — в нашем `ci.yml` он есть,
  и после него гейт укладывается в 1.1 с. Если объявляешь у себя гейт, который ходит в сеть,
  знай: красный у него может означать сеть, а не код

## [0.2.1] — 2026-09-04

### Исправлено

- `commit-explains-itself` краснел на каждой записи журнала, сделанной командой `aqk note`:
  `note` коммитит запись сама, и разделов `Сделано:`/`Не уверен:` в теле такого коммита нет.
  Кто поставил обе части комплекта из 0.2.0, видел красный прогон после каждой записанной шишки.
  Теперь коммит, не выходящий за пределы журнала, отчёта не требует — сама запись и есть отчёт
- Тот же гейт читал путь журнала из манифеста своим разбором и не резал комментарий в конце
  строки, тогда как программа его режет. Один файл — одни правила чтения

## [0.2.0] — 2026-09-04

Сплошное чтение программы и проверок. **Тем, кто поставил 0.1.0, обновиться стоит:** две
исправленные ошибки касаются безопасности — гейты молчали там, где должны были краснеть.

### Исправлено — безопасность

- Настоящий секрет в пользовательской папке `red/` или `green/` был невидим семи гейтам сразу:
  исключение образцов сравнивалось по имени папки, а не по пути. Теперь по пути
- Заготовка, которую выдаёт `aqk new`, несла ту же дыру — новый гейт рождался слепым
- `lesson-has-outcome` не видел ни одной записи журнала нового формата и молча пропускал всё

### Исправлено

- `always: false` в триггере молча игнорировался — условие читалось как «ограничения нет»
- `note` принимал запись, где слово «вывод» есть, а отметки решения нет
- `deps-are-pinned` требовал `go.sum` от Go-модуля без зависимостей — там его не бывает
- `gates.sh` не замечал осиротевший `check.sh` без `gate.yml`
- Документированный «порог для конвейера» без `--run` не проверял гейты вообще: тишина была
  неотличима от успеха — ровно то, против чего весь стандарт

### Добавлено

- Гейт `commit-explains-itself`: тело последнего коммита обязано нести `Сделано:` и `Не уверен:`.
  Первая запись каталога, которая читает историю, а не дерево файлов
- `doctor --run` пишет `.aqk/last-run.md` — что реально сработало и за сколько, отдельно от того,
  что лишь объявлено в манифесте
- `doctor` печатает свою версию
- Шаблоны PR и issue, `SECURITY.md` с приватным каналом сообщений об уязвимостях

### Проверено

- Полная самопроверка: 45 проверок в `smoke.sh`, 14 модульных, 15 записей каталога
- Установка посторонним путём (`npx github:…`) на чистом репозитории

## [0.1.0] — 2026-09-04

Первый тег.

### Добавлено

- Восемь команд: `init`, `start`, `doctor`, `add`, `new`, `ratchet`, `find`, `why`, `note`, `blob`
- Каталог из 14 гейтов с красными/зелёными образцами и доказательством из журнала шишек
- Храповик долга — старое пропускает, новое нарушение не проходит
- Разбор пропущенной ошибки прогоном (`why`) и порядок «с нуля» (`start`)
- Разовая просьба про звезду/обратную связь после первого `init`/`start` на машине
- `.github/workflows/triage.yml` — разбор входящих issue/PR Claude Code по подписке, вердикт в Telegram

### Проверено

- Прогон на 7 скачанных чужих репозиториях (JS/TS/Python/Go, 18–4000 файлов) — найдены и
  исправлены 2 дефекта: конвенция тестов Go (`*_test.go`), обёрнутые внешние ссылки в markdown
- Полная самопроверка: 40 проверок в `smoke.sh`, 13 модульных тестов, 14 записей каталога

[0.4.2]: https://github.com/arsen-ask-lx/Agent_Quality_Kit/releases/tag/v0.4.2
[0.4.1]: https://github.com/arsen-ask-lx/Agent_Quality_Kit/releases/tag/v0.4.1
[0.4.0]: https://github.com/arsen-ask-lx/Agent_Quality_Kit/releases/tag/v0.4.0
[0.3.0]: https://github.com/arsen-ask-lx/Agent_Quality_Kit/releases/tag/v0.3.0
[0.2.5]: https://github.com/arsen-ask-lx/Agent_Quality_Kit/releases/tag/v0.2.5
[0.2.4]: https://github.com/arsen-ask-lx/Agent_Quality_Kit/releases/tag/v0.2.4
[0.2.3]: https://github.com/arsen-ask-lx/Agent_Quality_Kit/releases/tag/v0.2.3
[0.2.2]: https://github.com/arsen-ask-lx/Agent_Quality_Kit/releases/tag/v0.2.2
[0.2.1]: https://github.com/arsen-ask-lx/Agent_Quality_Kit/releases/tag/v0.2.1
[0.2.0]: https://github.com/arsen-ask-lx/Agent_Quality_Kit/releases/tag/v0.2.0
[0.1.0]: https://github.com/arsen-ask-lx/Agent_Quality_Kit/releases/tag/v0.1.0
