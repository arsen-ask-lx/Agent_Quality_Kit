name: ci
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: npm ci
      - name: тесты
        run: pytest || true
      - name: типы
        run: tsc --noEmit
        continue-on-error: true
