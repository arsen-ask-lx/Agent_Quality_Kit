# Проверка в хуке выносит вердикт сама: без гашения её провал останавливает коммит.
# Служебная команда под «|| true» законна — это идемпотентность, а не выключенная проверка.
git add "packages/.stylelintrc.cjs" >/dev/null 2>&1 || true
pnpm lint-staged
