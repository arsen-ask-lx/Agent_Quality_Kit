.claude/settings.local.json
node_modules/
*.log
.DS_Store
/.aqk/

# собирается командой aqk blob, в историю не кладём
GOD_AI.md

# прогоны на чужих репозиториях, см. AGENTS.md — не наш код
/testbed/

# Песочница: чужие репозитории, скачанные для замеров. Никогда не коммитятся — это чужой код,
# и он бы утроил размер репозитория. Заводится командой из incidents (замер записей каталога).
sandbox/

# Сырьё из других проектов владельца (audit_project живёт только в GitLab) — в публичный репозиторий не попадает никогда.
research/inbox/
