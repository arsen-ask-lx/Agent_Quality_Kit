# AQK — техническое задание

> **Что это за файл.** Что мы строим, для кого, и по какому признаку поймём, что получилось.
> Состояние проекта и очередь работ — в `README.md`. Правила работы — в `AGENTS.md`.
>
> Файл живой: меняется вместе с пониманием задачи, но не задним числом. Решение поменялось —
> дописываем, а не переписываем.

---

## 1. Боль

Человек пишет код агентом. Агент ошибается: оставляет отладочную печать, плодит мёртвый код,
делает файл на девять тысяч строк, подгоняет тест под сломанную реализацию. Человек ловит это
глазами — то есть не ловит, потому что кода появляется больше, чем он успевает читать.

Когда человек всё-таки понимает, что нужен сторож, он делает его руками. В следующем проекте —
делает заново. Опыт не переносится: он в голове и в чужом репозитории.

По репозиторию **нельзя машинно узнать**, какие сторожа в нём есть, работают ли они и на чём
здесь уже обжигались. Это нужно четырём независимым сторонам: инструменту агента, конвейеру,
новому человеку, аудитору. Там, где сторон несколько, нужен стандарт, а не библиотека.

## 2. Что строим

**Тёмная фабрика:** цех, где работа идёт без людей у станка. Машине не нужен свет — ей нужно
видеть, что она делает, ловить собственный брак и уметь остановиться.

Из этого следует главная особенность: **все инструкции пишутся для агента, а не для человека.**
Человек смотрит на список дыр и отправляет агента их закрывать.

### Одно движение, из которого следует всё остальное

**Обещание проекта превращается в команду с кодом возврата.**

Было: «мы не оставляем отладочную печать» — текст, который агент может проигнорировать.
Стало: команда, которая возвращает не ноль. После этого обещание держит машина, а не добрая воля.

### Три надстройки над этим движением

| Что | Зачем |
|---|---|
| **Каталог** | чтобы не выдумывать команду заново в каждом проекте |
| **Уровни** | чтобы измерить, сколько обещаний уже машинные, и сравнить проекты |
| **Обмен** | чтобы обещания приносили другие люди, а не только автор |

## 3. Для кого

| Кто | Что хочет |
|---|---|
| владелец проекта, пишущий код агентом | список дыр и порядок их закрытия |
| агент | команду, которую можно выполнить, и образец «как правильно» |
| сторожа про качество кода | размер файла · проглоченная ошибка · маркеры «доделать потом» · закреплённые версии — все с образцами и доказательством из журнала |
| комплект держит собственный стандарт | **AQK-3**: всё объявленное прогоняется — `node tool/program.mjs doctor --run --min 1` |
| осмотр репозитория: 9 признаков, составные условия | в проекте без конвейера запись про конвейер скрыта с причиной — проверка в `smoke.sh` |
| конвейер | порог: пускать слияние или нет |
| человек, пришедший в чужой проект | по каким правилам тут живут |

## 4. Четыре сценария

### 4.1. Кода ещё нет

Человек получает порядок: сначала задача словами, потом ограничения, потом сайзинг, потом
архитектура. И сразу набор сторожей дня 0 — их ставят **до первой строки кода**, потому что
сторож, добавленный потом, делает весь старый код красным.

### 4.2. Код уже есть

`doctor` осматривает репозиторий сам и выдаёт три списка:

- **держит машина** — проверено запуском, а не наличием файла;
- **применимо и не поставлено** — вот команда, вот как поставить;
- **не применимо** — и почему именно.

Третий список — самое важное. Проект на пятьсот строк не должен видеть требование про
наблюдаемость: иначе человек закроет и не вернётся.

### 4.3. Поймал ошибку

Агент сделал файл на девять тысяч строк. Вопрос: почему не поймали? Ответ один из трёх:

- сторожа не было → поставить;
- сторож был, но не сработал → починить и добавить образец;
- сторож был, но его обошли → перенести из текста в механику.

Дальше решение человека: это его частность или общий случай. Общий — идёт в каталог.

### 4.4. Обмен знанием

Перед записью проверяем, нет ли уже такого. **Сверка идёт по намерению, а не по тексту команды:**
«отладочная печать не доезжает до прода» — одно намерение, а `ruff`, `eslint` и свой поиск —
три исполнителя одного намерения. Принёс рецепт под новый язык — это строка в существующей
записи, а не новая запись.

## 5. Устройство

### Гейт — это папка

```
gates/no-print-in-prod/
  gate.yml     намерение · арбитр по стекам · триггер · доказательство
  check.sh     переносимая проверка, если родного инструмента нет
  red/         код, на котором обязан сработать
  green/       правильный код, на котором обязан молчать
  README.md    зачем это и какой отказ поймало
```

### Почему именно шесть полей, а не галочка

Галочка «внедрено» отвечает не на тот вопрос: она означает «пакет стоит», а нужно «сторож
работает». Разрыв между этими утверждениями и есть главный дефект любого чек-листа.

### Почему обязателен триггер

Каталог может вырасти до сотен записей — конкретный проект должен увидеть десяток. Это и
отличает каталог от списка: список показывает всё, каталог с триггерами — применимое.

**Условие обязано быть запросом к репозиторию**, который умеет вычислить программа. «Внедрить,
когда появятся модели данных» не сработает: никто не сравнивает список с реальностью.

### Почему обязательно доказательство

Порог приёма: названо, какой конкретный отказ запись поймала. «Это хорошая практика» не
принимается. Без этого порога каталог за полгода набирает триста правдоподобных пунктов, среди
которых нельзя выбрать, — и умирает, как умерли awesome-листы.

## 6. Критерий успеха

Не «выпущена спецификация» и не «написан стандарт», а:

1. **Чужой человек поставил комплект в свой проект и закрыл хотя бы одну дыру.** Это первый
   честный сигнал; до него всё остальное — личный набор с амбициозным названием.
2. **Кто-то принёс свой гейт**, и он прошёл машинный фильтр.
3. **Уровень AQK сравним между проектами** и не требует доверия к автору.

## 7. Чего НЕ делаем

| Чего | Почему |
|---|---|
| правил, которые нельзя проверить машиной | уровень означал бы доверие к автору, а не факт |
| ступеней без кода, который их считает | сначала проверка, потом строка в таблице |
| зависимостей у программы | ставится в чужой проект, обязана читаться глазами за один присест |
| привязки к одному инструменту агента | обещание, которое держит только Claude Code, — не обещание |
| спецификации раньше практики | тридцать первый заброшенный репозиторий с манифестом и нулём пользователей |
| десятого пункта «для полноты» | раздувание = смерть |

## 8. Что сделано

| Что | Чем доказано |
|---|---|
| формат записи гейта | все записи каталога проходят машинный фильтр — `bash tool/selfcheck/gates.sh` |
| машинный фильтр на входе | подсунут заведомый брак — отклонён с названной причиной |
| сторожа про качество кода | секреты · печать · размер файла · проглоченная ошибка · маркеры долга · версии · сложность · дубли · мёртвый код — все с образцами |
| правило «сперва готовое» | карта из 964 правил `ruff`, в каждом README раздел «готовый аналог»: есть или нет и что проверено |
| работа на любом стеке | переносимые рецепты прогнаны на Python, TypeScript, Go и Rust |
| комплект держит собственный стандарт | **AQK-3**: два реестра долга под храповиком, всё объявленное прогоняется на себе |
| осмотр репозитория: 9 признаков, составные условия | в проекте без конвейера запись про конвейер скрыта с причиной — проверка в `smoke.sh` |
| три списка в `doctor` | прогон в чужом проекте |
| `doctor --run` — запуск гейтов | первый же прогон нашёл поломку в самом комплекте |
| `add` — постановка гейта одной командой | полный цикл: поставил → покраснел → починил → зелёный |
| храповик долга — `ratchet` | прогон: старое пропускает, новое не пускает, исправленное вычёркивает |
| сверка по намерению — `find` | описание чужими словами находит запись; несуществующее намерение не выдумывается |
| `note` — журнал не принимает брак | запись без вывода отклонена |
| петля «шишка → гейт» видна и сторожится | у каждой записи журнала — отметка исхода, а не рассказ без вывода. Проверяет `lesson-has-outcome` |
| комплект ставится одной командой | прогон на чистой папке — `bash tool/selfcheck/smoke.sh` |
| модульные проверки функций | написаны с красного, нашли три дефекта программы — `node --test tool/selfcheck/units.mjs` |
| конвейер | прогон на чужой машине при каждом пуше; доказан красным образцом — сломанная программа уронила его за 13 с |
| процедура приёма вклада | `find` сверяет, `new` даёт заготовку, машина отклоняет незаполненную; порядок в `CONTRIBUTING.md` |
| прогон по настоящим проектам | три прогона: 36 664 файла, 414 файлов, искусственные проекты на Go и Rust |
| короткая спецификация | `SPEC.md` — устройство отдельно от нашей реализации |
| разбор ошибки — `why` (сценарий 4.3) | три случая различает прогон, а не память: сторожа не было · не сработал · обошли. Пять проверок в `smoke.sh` |
| порядок «с нуля» — `start` (сценарий 4.1) | на пустом проекте ставит девять сторожей дня 0, все зелёные: долга нет. Три проверки в `smoke.sh` |
| программа держит собственный предел размера | 1357 строк → восемь файлов, каждый под 500; храповик вычеркнул долг сам |
| уровень своих проектов измерен | `audit_project`, Newsletter, Amplifie — манифеста нет ни у одного, уровень не заведён |
| прогон на чужих репозиториях (суррогат чужого пользователя) | `start`/`doctor`/`add`/`ratchet` на 7 скачанных репозиториях (JS/TS/Python/Go, 18–4000 файлов): найдено и исправлено 2 реальных дефекта |
| просьба про звезду и обратную связь | печатается один раз на машину после `init`/`start`, отметка вне git — проверено `smoke.sh` |
| разбор входящих issue/PR нейросетью | `.github/workflows/triage.yml`: Claude Code по подписке решает, стоит ли смотреть, шлёт вердикт в Telegram; прав на запись в GitHub нет — проверено живым прогоном (issue #7) |
| сплошное чтение программы и проверок | найдено и закрыто 14 дефектов, из них два про безопасность: реальный секрет в пользовательской `red/` был невидим семи гейтам сразу, и та же дыра ехала в заготовке `aqk new` |
| `doctor --run` пишет `.aqk/last-run.md` | что сработало и за сколько — отдельно от того, что лишь объявлено в манифесте |
| коммит несёт мини-отчёт | гейт `commit-explains-itself` требует `Сделано:` и `Не уверен:` в теле последнего коммита — первая запись каталога, читающая историю, а не дерево файлов |
| порядок выпуска версий | SemVer, `CHANGELOG.md` по Keep a Changelog, шаблоны PR и issue, `SECURITY.md` с приватным каналом сообщений |
| комплект выпускается сам от метки | доверенный издатель npm (OIDC): ни токена, ни второго фактора на машине; пакет с удостоверением происхождения, релиз на GitHub собирается из журнала изменений |
| ставится одной короткой командой | `npx agent-quality-kit doctor` — пакет 230 КБ вместо клона репозитория |
| интерфейс и файлы проекта на двух языках | вывод, намерения записей каталога, `AGENTS.md`/`CLAUDE.md`/манифест; расхождение каталогов строк сторожит модульная проверка |
| **первый прогон в чужом живом проекте** | Django на Windows: найдено 4 дефекта, которых не нашла ни одна наша проверка — родные рецепты недостижимы на Windows при зелёном прогоне, `note` пишет в чужой репозиторий, настройка исключений правкой затираемого файла, отчёт агента по памяти |
| обязательная форма отчёта | `aqk report` собирается прогоном: какой рецепт выбран (родной или переносимый), что стоит слабее возможного, что скрыто `.aqkignore`, что велено прочитать. Красный гейт даёт код возврата 1 |
| исключения проекта отдельно от инструмента | `.aqkignore` читается общим `_skip.sh` — действует на все сканирующие записи, включая уже установленные копии |

## 9. Что осталось

По убыванию отношения пользы к цене:

| # | Что | Почему это важно | Кто может |
|---|---|---|---|
| 1 | **Пользователь, который не мы** | первый живой прогон был в проекте владельца и дал 4 дефекта за раз. Это доказало метод, но не снаружи: человек, которому комплект ничем не обязан, проверит остальные утверждения разом | только человек |
| 2 | **Конвейер на windows-раннере** | самый тяжёлый дефект был именно там и жил, пока его не нашли чужие руки. Пока прогона на Windows нет, «работает на Windows» — рассуждение, а не факт | решение владельца |
| 3 | **Доказательство записи про секреты** | три прогона по живым проектам дали ноль находок; запись остаётся условной под храповиком | появится с настоящей находкой |
| 4 | **Проверки, зелёные по совпадению** | одна проверка `.aqkignore` была написана так, что не могла покраснеть, и это заметили случайно. Сколько таких ещё — неизвестно, а проверка, которая не может покраснеть, неотличима от работающей | нужен способ мерить, а не читать |

## 10. Честный риск

Если идея хорошая, через год похожее выпустит кто-то крупный, и его версия победит по
дистрибуции. Ближайшие соседи существуют уже сейчас: автоматический скоринг репозитория и значок
соответствия со ступенями. Ни один из них не про готовность к работе агентов — но механизм и
дистрибуция у них есть, и фора измеряется месяцами, а не годами.

Защита одна: **быть первым, кто описал сложившуюся практику**, и держать реализацию лучшей.
Не гнаться за объёмом спецификации — гнаться за тем, чтобы ею было удобно пользоваться.
